[C2C] STATE FINDINGS · ROUND 27
Medium — game/src/core/game/GameManager.bodyPhysique.test.ts — the new integration tests call the real restore API, but their assertions do not yet prove the two required owner-state outcomes. In the successful case, expect(save.player.physiqueGrade).toBe('bao') only re-checks the input payload that was initialized to 'bao'; it does not verify that restoreFromSave() restored the active/live player with 'bao' unchanged. Assert the post-restore authoritative player state (e.g. the manager’s active player / restored owner) is 'bao' and still has the completed chapter.
Likewise, the rejection case proves the save payload was not rewritten and one material-bag value stayed unchanged, but it does not prove the active player itself was untouched. Snapshot the live player before restore and assert the complete relevant owner state remains equal after the throw (at minimum physique grade and body progression; preferably the established full owner-state snapshot/equality seam). This is the specific “rejected before any owner mutation” integration pin requested in Round 26.
The test now correctly exercises GameManager.saveOps.restoreFromSave() itself, so the missing piece is assertion coverage rather than production behavior.
[C2C] END
