[C2C] STATE FINDINGS · ROUND 24
High — game/src/core/game/GameManagerRealmAdvanceOps.ts — the new preflight uses structuredClone(player), which is unsafe for the live Pinia call path. This repository already had to avoid structuredClone for player state in the RESPEC dry-run because Pinia reactive proxies are not structured-cloneable and can throw DataCloneError. If investBodyChapter() receives the normal reactive player.$state/store-backed object, every essence investment can fail before reaching the planner. Use the save-shaped JSON clone already established for PlayerData, or explicitly clone a non-reactive/raw serialization. Add a regression test through the actual UI/store-facing ops call, not only a plain-object fixture.
Medium — game/src/core/simulation/earlygame/EssenceSubstitutionEconomy.ts / game/src/data/realm/PhysiqueEssence.ts — Round-17 finding #3 is still unresolved in the supplied production diff. No simulation/ratio-authority production change is shown. The previous implementation explicitly allowed measureEssenceRatioLock() only for qi_refining, while production authored both bao: 2 and phap: 2. Round 17 allowed either an independently exercised downstream lock or an explicit normative derivation of Pháp→Bảo from the first-hop invariant. Neither appears in this production diff. If the commit resolves this solely by amending the mission spec, that amended normative text must be part of the reviewed change; otherwise phap: 2 remains copied rather than sim-locked/derived by contract.
The Round-17 transaction-order and surplus-loss defects are otherwise structurally fixed: progression is first evaluated on a detached probe; debit sufficiency and complete required-grade change capacity are checked before bag mutation; then debits/change commit before applying progression to the real player. The change-capacity calculation correctly accounts for any required-grade debit before crediting surplus, so successful commits no longer intentionally discard conversion change.
[C2C] END
