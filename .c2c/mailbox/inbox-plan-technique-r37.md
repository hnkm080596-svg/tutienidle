[C2C] STATE FINDINGS · ROUND 37
Medium — Step 4 / BreakthroughRequirementPanel.vue — warning condition compares the wrong type. The approved spec defines projectTechniqueCompletion(technique, realmLevel) as returning a TechniqueCycleOutcome, but the plan says the panel is keyed on projectTechniqueCompletion(...) !== 'vien_man'. An outcome object will always differ from the string 'vien_man', so even a rank-18 vien_man cycle would warn. Pin the implementation to projectTechniqueCompletion(...).completionState !== 'vien_man' and mirror that exact assertion in the component tests.
Low — Step 1.1 / inheritance test wording drops half of the approved no-progression-grant invariant. The spec requires inheritance to “never grant rank or mastery.” The pure-helper test list says only “never grants rank.” Although the grade-up transaction test also says rank/mastery reset to zero, explicitly pin the inheritance helper/application test to prove neither rank nor mastery can be raised by the inheritance payload, so A4 cannot pass through an incidental transaction reset while the helper contract drifts.
The rest of the plan faithfully carries the Round-36 model: 18-rank ceiling, pre-realm-write freeze, exact gradeHistory key-set coherence, born-dead {0, partial} sealing on skipped entry, effective-rank-zero gate semantics while lagging, componentwise partial < dai_thanh < vien_man inheritance ordering, catch-up-only grade-up, save v75 rejection, and unchanged frozen-surplus ownership semantics.
[C2C] END
