[C2C] STATE FINDINGS · ROUND 22
Medium — game/src/core/realm/body/BodyProgressionSystem.ts — the new physique-completion seam is not wired to restore/reconciliation, so the “completion advances exactly once, persisted, restore-safe” contract is only guaranteed on the live invest path. applyPhysiqueAdvancement() is extracted correctly, but the shown production diff invokes it only inside investBodyChapterState() after consumed > 0. A restored state with a completed authored chapter and physiqueGrade === advancement.from will not advance unless some later successful investment happens—and for an already-complete chapter, that may never occur. If restore safety means canonical completed chapter state must reconcile the persisted physique transition, the restore/body-integrity path must call this same seam once after chapter-state validation. If the intended rule is instead “the persisted physiqueGrade is authoritative and restore never derives it from chapter completion,” the spec/tests must pin that explicitly; otherwise the current implementation has two conflicting readings of restore behavior.
Low — game/src/core/realm/body/BodyChapter.ts — registry validation does not validate the authored chapterKind classification beyond vocabulary membership. A chapter with id: 'body_refinement' and chapterKind: 'meridian' passes as long as the string is one of refinement | meridian | zhou_tian. Since this mission makes chapter kind first-class specifically as registry classification for later dispatch-independent extensions, malformed kind assignment should fail authoring validation, either through per-ID expected classification for current chapters or through kind-specific structural contracts as those kinds are introduced. The current validator proves “declared kind exists,” not “classification is correct.”
The authority boundary is otherwise sound in the shown diff: player.physiqueGrade remains the sole persisted physique state; physiqueAdvancement stays authored transition metadata rather than a second state source; advancement remains exact-from guarded and therefore idempotent; realm and essence-band mappings are not used to derive physique. The base-stat seam is also correctly generalized from emission-kind dispatch to the optional collectBaseStatDeltas capability, allowing later chapter kinds to contribute without adding another stat-emission discriminant.
[C2C] END
