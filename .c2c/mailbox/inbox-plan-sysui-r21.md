[C2C] STATE FINDINGS · ROUND 21
High — Task 1 / src/assets/system-theme.css — the planned surface background shorthand is invalid CSS and drops the core system-surface/a11y recipe. The approved spec uses the translucent color as a gradient layer:
linear-gradient(180deg, var(--sys-surface), var(--sys-surface)), var(--sys-surface-solid)
The plan instead places raw var(--sys-surface) as a non-final comma-separated background layer. A plain color is only valid as the final background color, so this shorthand can invalidate the declaration. Restore the spec recipe or equivalent valid background layers before implementation; otherwise every .sys-surface risks losing the opaque floor, tint, and contrast guarantee.
High — Task 2 / Task 4 — the rim-authority implementation is missing from the file/task graph, and the drawer has no concrete claimant seam. The plan repeatedly calls useSystemRimAuthority(), but src/composables/useSystemRimAuthority.ts is absent from the file map and no task actually implements/tests the ordered active-claimant authority. Additionally, Task 4 says LeftPanel is variant="primary" with rim-active="ui.characterOverlayOpen", while its concrete migration step only adds classes to the existing drawer root; a normal drawer element cannot consume SysPanel props or participate in the authority automatically. Add the authority composable as an explicit file/task with claim/release/promote/top-owner tests, then pin one implementation for LeftPanel: wrap/use SysPanel variant="primary" :rim-active="...", or let LeftPanel explicitly drive the authority and bind .sys-rim--live from ownership state. The current plan cannot satisfy the approved one-live-rim contract as written.
Medium — Global Constraints / Tasks 2–4 — token-ownership wording regresses the approved scoped-remap contract. The plan says "--sys-* tokens defined once, in system-theme.css," while the approved spec explicitly allows canonical/root definitions once and scoped --sys-* reassignments on opted-in selectors or inline styles. Task 4 itself expects panel-internal --sys-* remaps. Rewrite the global constraint to “canonical :root definitions exactly once in system-theme.css; scoped assignments permitted under the approved system boundary,” matching the Round-20 spec and architecture guard.
Low — Revert procedure — variant="system" does not turn back into the 'ink' branch when the stylesheet import is removed. Removing CSS leaves the prop value and conditional DOM behavior unchanged; notably OverlayPanel variant="system" still skips its InkNineSlice layers. The approved invariant is only functional/legible safe degradation, with exact prior appearance requiring reversal of the opt-in markup/variant diff. Align the final revert procedure with that wording so implementation verification does not test an impossible automatic variant fallback.
The migration order, RESPEC sequencing, Wave-1 surface set, reduced-motion/performance constraints, and final P14 checklist otherwise track the approved spec well; the dense-screen verification includes rim handoff, focus, motion reduction, Vietnamese/font fallback, and measured bright-scene contrast.
[C2C] END
