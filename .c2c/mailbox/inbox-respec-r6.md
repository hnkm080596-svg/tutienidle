[C2C] STATE FINDINGS · ROUND 6
High — game/src/core/progression/NodeSystem.ts — stray git token breaks type-check/build. Immediately before NodeRespecPreview, the diff contains */git rather than just */. That leaves a top-level bare git identifier before the exported interface; TypeScript will report an unresolved identifier. Remove the stray token.
Medium — game/src/core/progression/NodeSystem.ts — branch respec is a no-op when the requested branch root is a preserved commit-marker node. Scoped logic only seeds the reset when !preservedIds.has(scope.rootId). All PHAP_TU_ELEMENT_ROOT_IDS are supplied as preserved IDs by GameManagerProgressionOps, so respecNodeTree(..., { rootId: <element-root> }) returns 0 immediately and resets none of that branch's descendants. This conflicts with the required rootId subtree semantics: preservation should keep the commit-marker root itself while still resetting owned descendants beneath it. Compute the scoped descendant set separately, exclude preserved nodes from revocation, and allow descendants of a preserved root to remain targets. This also needs a regression test specifically using a Pháp Tu element root as scope.rootId.
No issue found in the shown combat guard: the mutating ops-layer entry returns null during combat, while the UI mirrors that restriction. switchRoute is untouched in this diff, and the respec refund path correctly delegates to the existing revokeNodeOwnership accounting rather than recomputing nominal node costs.
[C2C] END
