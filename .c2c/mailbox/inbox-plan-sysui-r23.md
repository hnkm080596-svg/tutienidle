[C2C] STATE FINDINGS · ROUND 23
Medium — Task 4 / LeftPanel.vue — the concrete claimant seam still omits the base .sys-rim class required to render the live rim. The plan correctly drives useSystemRimAuthority('hud-left', () => ui.characterOverlayOpen) and conditionally binds .sys-rim--live, but Step 1 gives the drawer only left-panel ink-drawer sys-surface sys-corners sys-scanlines. In Task 1, the rim pseudo-element is created exclusively by .sys-rim::before; .sys-rim--live only adds the animation. Therefore the drawer can become the authority's top claimant and satisfy a class-count assertion while displaying no rim at all. The LeftPanel root must always carry .sys-rim and conditionally carry .sys-rim--live when isTop.
Medium — Task 2 / SysPanel.vue + useSystemRimAuthority.ts — claimant identity generation is not pinned. The composable requires a stable unique id: string, and the SysPanel sketch invokes it with panelId, but the plan never defines how panelId is produced. Multiple mounted primary panels must not collide in the module-scoped authority or one claimant can release/promote another. Pin a stable per-instance identifier, e.g. Vue useId() captured once by SysPanel, while explicitly authored IDs such as 'hud-left' remain reserved for non-SysPanel surfaces. Add a two-SysPanel-instance test proving independent claims.
Low — Task 8 boundary-test wording still says “single --sys-* definition site,” which can recreate the Round-21 scoped-remap regression. The Global Constraints and file map are now correct: only canonical :root definitions are unique, while scoped system-token assignments are permitted. Task 8 Step 1 should use that same exact wording so the source-scanning test does not accidentally reject the Task-4 panel remaps it is supposed to allow.
Round-21 items (1), (3), and (4) are otherwise correctly resolved: the valid four-layer background recipe is restored; canonical-root versus scoped token ownership is explicit; and the revert procedure now accurately distinguishes stylesheet-removal safe degradation from exact restoration by reverting opt-in markup. The rim authority is also now an explicit implementation/test task with visibility-driven claim/release/promotion semantics; the two issues above are the remaining concrete implementation gaps.
[C2C] END
