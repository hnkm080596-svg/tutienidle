[C2C] STATE FINDINGS · ROUND 29
Medium — game/docs/p7/missions/mqi-09-essence-substitution.spec.md §3.3 — normative transaction text still mandates structuredClone, contradicting the Round-24 fix and the now-correct production implementation. The production code has appropriately switched the probe to JSON.parse(JSON.stringify(player)) as PlayerData, and the new Pinia $state integration test directly exercises the live reactive call path that previously risked DataCloneError. However, the reviewed spec amendment still says the essence branch “computes consumed on a structuredClone probe FIRST.” That would instruct a future implementation/refactor to reintroduce the exact defect fixed in this round. Update the normative text to require a detached save-shaped/plain PlayerData probe, explicitly using the JSON/detach strategy compatible with reactive Pinia state rather than structuredClone.
Round-24 finding #1 is otherwise resolved in production: the probe now reads through reactive Pinia proxies via JSON serialization, and the new test passes the actual Pinia $state through investBodyChapter, verifying successful substitution and real progression rather than only a plain-object fixture.
Round-24 finding #2 is resolved by the supplied normative amendment. §3.4 now explicitly rules that downstream adjacent hops inherit the first measurable Bảo→Phàm lock until a downstream band-specific chapter/arc becomes measurable, with mandatory re-locking once that evidence exists. That satisfies the previously permitted “explicit derivation rule” path and no longer represents an unexplained copied phap: 2 constant.
The prior validate → debit/change-capacity preflight → bag commit → real-player apply ordering remains intact; no new transaction or rounding defect is evident in the supplied production change.
[C2C] END
