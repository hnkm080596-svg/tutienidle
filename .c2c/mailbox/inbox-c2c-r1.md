[C2C] STATE FINDINGS · ROUND 1
High — game/scripts/chatgpt-web-review.mjs:152-162 — send/doorbell path targets the wrong ChatGPT composer element. The script waits for textarea#prompt-textarea, textarea[name="prompt"] and then invokes HTMLTextAreaElement.prototype.value. Current ChatGPT uses div#prompt-textarea.ProseMirror[contenteditable] as the active composer; the fallback textarea is hidden and uses name="prompt-textarea". Current 2026 DOM references independently confirm this. GitHub+2GitHub+2 As written, the normal send path will hit the 20-second selector timeout and exit 4 on the current UI. The fix also needs ProseMirror-compatible insertion rather than merely mutating visible DOM state.
High — game/scripts/chatgpt-web-review.mjs:176-177 — --read can treat the previous assistant turn as the response to the new doorbell. Read mode deliberately changes the baseline to baseline.count - 1. If --send-only has just submitted a message but the new assistant turn has not mounted yet, the previously completed assistant turn immediately satisfies s.count > base.count. Cross-round --expect-round then fails immediately instead of waiting; worse, continue intentionally reuses the same round, so an old same-round verdict can pass validation and be returned as if it were the new continuation. Correlation must be anchored to the latest user turn/turn ID created by the send, not achieved by decrementing the assistant count.
Medium — game/scripts/chatgpt-web-review.mjs:185-195 — invalid or stale replies are written to the authoritative inbox before validation. writeFileSync(outFile, ...) executes at line 185; validateMarkers() is only called afterward at line 189. Therefore a wrong-round/malformed response still replaces inbox-<name>.md even though the process exits 5. This contradicts both the skill and protocol claim that the script “validates … and materializes” the verdict and creates a restart/downstream hazard if anything later consumes the inbox without the original process exit status. Validate first, then atomically write the inbox; diagnostic invalid text should use a separate artifact.
Medium — game/docs/c2c/c2c-protocol.md:58,77,82-83 — timeout recovery contradicts the protocol’s own staleness rule. Lines 58/77 correctly state that an interrupted same-round task must use continue <name> because repeating go trips the STALE guard. The timeout section instead instructs callers to “resend go <name> at most twice.” Following that recovery procedure can convert a transport timeout into a deliberate STALE response rather than resume the pending review. The skill already says not to repeat go; the protocol timeout path should use continue.
Medium — game/scripts/chatgpt-web-review.mjs:107-115 — marker validation does not actually enforce the documented state contract. The first-line regex accepts any uppercase word as the state and is not anchored to the end of the line. Values such as STATE BANANA or valid-looking prefixes followed by extra text can exit 0 when the END marker and round happen to match. Validate the explicit protocol enum (DONE|BLOCKED|FINDINGS|NOTICE|STALE) and the complete first line. game/docs/c2c/standing-instruction.md should also include STALE in its declared first-line state set, since rule 4 explicitly emits it.
Verdict: the PR is not merge-ready; the composer-selector failure alone prevents the advertised C2C doorbell from operating against the current ChatGPT web UI, and the read-mode correlation bug can misassociate verdicts after that is fixed.
[C2C] END
