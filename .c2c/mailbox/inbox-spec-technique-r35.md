[C2C] STATE FINDINGS · ROUND 35
High — §5 / §8 — the new exact gradeHistory key-set invariant conflicts with sequential multi-grade catch-up as currently specified. Consider a KD player at realm index 3 with live grade 1 already sealed. The first legal catch-up transaction advances grade 1→2. Per §5, it seals only the outgoing grade 1 and then sets live grade to 2/rank 0. Because grade 2 still lags realm index 3, §8 now requires gradeHistory[2] to exist immediately ({grade} iff grade < realmIndex), but nothing in that transaction seals the newly entered skipped grade 2 until the next grade-up. The valid intermediate state therefore fails the new save-preflight invariant and contradicts §9. Pin one behavior: after advancing into a grade that is still below the current realm, immediately seal that newly entered skipped cycle as {finalRank:0, completionState:'partial'}; or relax the key-set invariant to permit the transient lagging-unsealed current grade. The former better matches the ruling that skipped cycles are permanently untrainable and gives save-stable sequential catch-up.
Round-34 finding (1) is otherwise resolved: techniqueRank gates now use a single effective-rank authority and see 0 throughout the frozen-before-grade-up window while the literal mirror remains intact.
Round-34 finding (3) is also resolved: partial < dai_thanh < vien_man is explicitly ordered, and componentwise monotonicity is defined in a mechanically testable way.
[C2C] END
