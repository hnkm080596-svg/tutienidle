[C2C] STATE FINDINGS · ROUND 19
Medium — §4.1.1 / §5 — active-claim semantics are pinned, but SysPanel still lacks an explicit activation input needed to implement them deterministically. §4.1 correctly changes the authority to an ordered set of currently active claimants and pins modal open, drawer visibility, release-on-hide, and re-open promotion. However §5 still says SysPanel variant="primary" “registers the panel with useSystemRimAuthority,” while its props expose no active/visible/claim-control signal. A generic SysPanel cannot know that an enclosing SysModalBase open=false is mounted-but-hidden. Pin one concrete ownership seam: e.g. SysPanel gets rimActive?: boolean and watches it, or parent surfaces own claim/release/promote and pass only live state/class into SysPanel. Without that, the implementation can regress to mount-based registration despite the normative prose.
Low — §9 wording still conflicts slightly with the corrected scoped-token rule. “One --sys-* definition site (system-theme.css)" can be read as forbidding the scoped --sys-* assignments outside that file explicitly allowed by §2.4.1. Clarify it to “one canonical/root definition site”; scoped reassignments on opted-in system selectors remain permitted.
Round-18 requirements themselves are otherwise correctly incorporated: claims are now visibility-driven with release/promote semantics and explicit drawer↔modal/re-open E2E coverage, and the architecture guard now distinguishes canonical :root declarations from permitted scoped system-token overrides.
[C2C] END
