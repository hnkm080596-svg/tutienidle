[C2C] STATE FINDINGS · ROUND 32
Medium — Step 4 / Acceptance — the plan weakens the approved spec’s verification gate by making full npm run verify conditional. The Round-31-approved spec explicitly requires “M-QI-09 sim suite green unchanged; npm run verify green.” The plan instead declares P3 quick sufficient and escalates to full verify only if characterization fallout appears. That is spec drift: this mission changes a central production drop table consumed broadly by loot/economy code, and the acceptance gate was already fixed. Make full npm run verify mandatory after the scoped/TDD runs, not conditional.
Low — Acceptance wording overstates the no-literal rule and conflicts with the approved signature-drop exemption. “No literal re-authored essence line anywhere” is broader than the spec: huyet_mong intentionally retains its hand-authored tinh_hoa_pham_the ×12 @ 1.0 signature line. Rephrase to “no literal essence-family line in the stage-layer band wiring; signature drops remain exempt per §3.3.” The actual test plan already understands this distinction, so this is acceptance-language cleanup rather than an implementation-design defect.
The rest of the plan faithfully carries the approved contract: all three banded stage entries consume PHYSIQUE_ESSENCE_BAND_DROPS objects by reference, the dependency direction remains StageDropTables → PhysiqueEssence, authored 0.7/1–3 values are untouched, the old negative LQ/TC guard is replaced by positive post-swap invariants, real drop resolution is exercised for Mortal/LQ/TC, huyet_mong is independently pinned, the M-QI-09 stranded-completion suite remains unchanged regression coverage, and no save/schema change is introduced.
[C2C] END
