[C2C] STATE FINDINGS · ROUND 14
Medium — game/src/core/artifact/ArtifactProgression.ts — Round-12 persisted-ownership fix is not actually demonstrated by this production diff. The only change inside normalizeArtifactProgress() is a comment; meetsAwakenGate still comes from isArtifactDomainUnlocked(player.realmId) exactly as in the flagged version. Therefore any pre-existing branch below the shown hunk that clears/replaces player.artifact when meetsAwakenGate === false remains unchanged. Adding the EXP-access guard in BattleLootSystem is correct, but it does not itself make restore normalization non-destructive. This round needs the corresponding beyond-ceiling restore test and/or an actual normalization change proving a matching persisted artifact survives unchanged. As shown, pin (3) cannot be considered fixed.
Low — game/src/data/ui/commandWheelCatalog.ts — the new release-lock reason introduces another hard-coded Vietnamese UI string. RELEASE_UNAVAILABLE_REASON = 'Chưa mở trong bản hiện tại' is new user-facing production text rather than an i18n key, so English UI will still display Vietnamese and it violates the project’s UI-i18n boundary. The semantic distinction requested in Round 12 is otherwise correct; expose a reason code/key and translate it at the presentation boundary.
Pins (1) and (2) are correctly fixed: isRealmTransitionEnabled() now requires exact adjacency plus target availability, so skip transitions fail closed; isBreakthroughAcquisitionEnabled() resolves the canonical predecessor and delegates to that same transition predicate, while unknown target IDs still fail closed and untagged resources remain unrestricted.
The command-wheel logic also now correctly distinguishes a normal below-Trúc-Cơ progression lock from a release-unavailable player realm. No new issue is evident in those predicate changes.
[C2C] END
