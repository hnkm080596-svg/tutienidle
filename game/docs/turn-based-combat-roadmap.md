# Roadmap Chuyển Đổi Combat — Real-time → Turn-Based

> Tài liệu định hướng riêng cho việc chuyển đổi combat từ real-time auto-battler sang turn-based ATB, lập ngày 2026-09-04.
> Mỗi hạng mục có spec/plan riêng (dẫn link bên dưới) — khi làm đến đâu, khảo sát + brainstorm + viết plan riêng cho đúng hạng mục đó, KHÔNG gộp nhiều hệ thống vào một plan (quyết định người dùng, 2026-09-04).
> Khi plan chi tiết và roadmap này lệch nhau, plan chi tiết là nguồn sự thật; cập nhật lại dòng tương ứng ở đây ngay sau đó.
> Tài liệu gốc: [2026-09-03-turn-based-combat-design.md](../../docs/superpowers/specs/2026-09-03-turn-based-combat-design.md) (spec thiết kế đã duyệt) + [2026-09-04-turn-based-combat-survey-and-stat-decisions.md](../../docs/superpowers/specs/2026-09-04-turn-based-combat-survey-and-stat-decisions.md) (khảo sát hệ thống thật + sửa sai lệch so với spec gốc + quyết định Stat).

## Nguyên tắc

- Dự án đang ở dev phase — không cần lo save migration (big-bang, không giữ tương thích ngược).
- Sản xuất/tu luyện (idle) **không đổi** — đây là rework thuần combat.
- Combat không bao giờ mô phỏng offline; "Auto" là chính engine turn-based chạy nhanh hơn, không phải công thức riêng.
- Hệ nhân vật phụ (party/companion) **ngoài phạm vi** toàn bộ rework này — có spec riêng sau.
- Mỗi hạng mục dưới đây khi đến lượt làm: khảo sát codebase hiện tại → brainstorm/chốt quyết định với người dùng → viết plan riêng (`docs/superpowers/plans/...`) → cập nhật dòng trạng thái ở đây kèm link plan.

## Milestone 1 — Foundation (primitives độc lập)

Mục tiêu: dựng các primitive thuần (pure function), test riêng, KHÔNG đụng vào `BattleSystem.ts`/`CombatSystem.ts` hiện có.

| Hạng mục | Plan | Trạng thái |
|---|---|---|
| ActionGauge, TurnQueue, ChannelQueue, AoeShape*, BounceChain, TrueShot, BossTurnTriggers, MomentumBreak, ResourceTurnHook — 9 file mới dưới `game/src/core/battle/turn/` | [2026-09-03-turn-based-combat-foundation.md](../../docs/superpowers/plans/2026-09-03-turn-based-combat-foundation.md) | 🟢 Xong, merge master 2026-09-04 (merge commit `aee253b`; 50/50 test turn/ suite, type-check sạch; QA quick report: [2026-09-04-turn-combat-foundation-quick.md](qa/2026-09-04-turn-combat-foundation-quick.md) PASS WITH EVIDENCE) |

## Milestone 2 — BattleSystem Replacement (gộp mọi hệ liên quan, 2026-09-04)

**Quyết định người dùng 2026-09-04**: không tách "Core System Conversion" thành milestone riêng nữa — không đáng phân biệt. Mọi hệ dưới đây làm CHUNG một đợt lớn xoay quanh việc thay `BattleSystem.update(deltaSeconds)`. Trong đợt này vẫn viết plan riêng cho từng hệ khi đến lượt (không gộp 1 plan khổng lồ), nhưng không còn khái niệm "làm sớm trước BattleSystem" nữa — trọng tâm hiện tại là chính bản thân BattleSystem Replacement.

### BattleSystem Replacement — chia slice (2026-09-04)

`update(deltaSeconds)` (18-bước pipeline, ~2400 dòng) quá lớn cho 1 spec/plan — chia thành các slice dọc, mỗi slice lớp thêm 1 phần hành vi thật lên trên slice trước, mỗi slice có spec+plan riêng.

| Slice | Nội dung | Spec | Plan | Trạng thái |
|---|---|---|---|---|
| Slice 1 — Core Turn Loop | Class mới `TurnBattleSystem` độc lập, headless: 1 player vs N enemy cố định, ATB turn order + targeting §4 (gần nhất-trước-mặt) + basic attack qua `CombatSystem.resolveActionHit` có sẵn. KHÔNG skill/buff/reaction/hazard/wave/UI/Stat speed thật (speed truyền tay qua constructor, tách khỏi quyết định Stat còn treo) | [2026-09-04-turn-battle-system-slice1-core-loop-design.md](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice1-core-loop-design.md) | [2026-09-04-turn-battle-system-slice1-core-loop.md](../../docs/superpowers/plans/2026-09-04-turn-battle-system-slice1-core-loop.md) | 🟢 Xong, merge master 2026-09-04 (merge commit `72eec64`; 8/8 plan tests + 4 adversarial probes, turn/ suite 63/63, type-check sạch; QA quick report: [2026-09-04-turn-battle-system-slice1-quick.md](qa/2026-09-04-turn-battle-system-slice1-quick.md) PASS WITH EVIDENCE) |
| Slice 2 — 3-Skill Action Model | **THAY hẳn bản 6-slot loadout cũ (2026-09-04, phiên sau)**: mỗi combatant (Player VÀ Enemy, đối xứng) có đúng 3 skill role cố định — `basicSkillId` (không cooldown), `specialSkillId`/`ultimateSkillId` (cooldown-lượt, resource-gate). Pháp Tu Thuần map thẳng vào chuỗi skill 1-hành đã có (`getPhapTuThuanElement`/`CHAIN_SKILL_IDS`) — không cần book. CHỈ skill damage thuần (không buff/debuff — hoãn Slice 3). Đổi API `runToCompletion()` → `resolveNextStep()` step-oriented tại đây. KHÔNG gồm migrate content thật (basic/special/ultimate của từng build cụ thể) hay migrate `Enemy.specialAttacks[]` — việc nội dung riêng | [2026-09-04-turn-battle-system-slice2-skill-actions-design.md](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice2-skill-actions-design.md) | [2026-09-04-turn-battle-system-slice2-skill-actions.md](../../docs/superpowers/plans/2026-09-04-turn-battle-system-slice2-skill-actions.md) | 🟢 Xong, merge master 2026-09-04 (merge commit `e3ade36`; TurnSkillAction 22/22 + adversarial 6/6, Slice 1 tests 11/11 giữ nguyên pass, turn/ suite 131/131, type-check sạch; QA quick report: [2026-09-04-turn-battle-system-slice2-quick.md](qa/2026-09-04-turn-battle-system-slice2-quick.md) PASS WITH EVIDENCE) |
| Slice 3 — Buff/CC Wiring + Zone-as-dot | **Khảo sát 2026-09-04 xác nhận `TurnBuffSystem`/`TurnBuffPool`/`TurnBuffTypes` ĐÃ MERGE XONG (Milestone 1, không chỉ có plan)**. Mỗi `TurnBattleParticipant` có `buffs: TurnBuffPool` riêng; `TurnSkillDefinition` thêm `appliesBuff?`; tick buff tại lượt của chính holder (TRƯỚC tickCooldowns/selectAction); CC (stun/freeze) block hành động. Zone-as-dot: skill AOE + `appliesBuff` trỏ `dot` buff = đủ thay Lava/Sword Zone, không cần `TurnHazardZoneSystem`. CHỈ dùng `dpsRatio` (không sửa TurnBuffSystem thêm dạng flat-damage). CHỈ fixture skill/buff (KHÔNG migrate content thật Dung Nham/Kiếm Trận). CHỈ dot+cc effect (KHÔNG statModifier/onHitProc — cần stats-recompute pass chưa có) | [2026-09-04-turn-battle-system-slice3-buff-design.md](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice3-buff-design.md) | [2026-09-04-turn-battle-system-slice3-buff-wiring.md](../../docs/superpowers/plans/2026-09-04-turn-battle-system-slice3-buff-wiring.md) | 🟡 Plan viết xong 2026-09-04, sẵn sàng giao agent thực thi (phụ thuộc Slice 2 đã merge trước) |
| Slice 4 — Resource/Boss Triggers | **BỎ HẲN MomentumBreak.ts (2026-09-04, phiên Slice 4)**: cơ chế Break/stagger cắt khỏi redesign — boss giờ chỉ là quái có mechanic riêng qua BUFF. Wire `ResourceTurnHook.applyTurnStartDeltas` (fixture `TurnResourcePool`, tick tại lượt CHÍNH holder) + `BossTurnTriggers.isTurnTriggerReady` (bộ đếm `totalTurnsElapsed` toàn trận, boss tự áp buff qua `TurnBuffSystem` sau N lượt, fire đúng 1 lần). Cả 2 hook UNCONDITIONAL với CC block (khác action). CHỈ fixture, KHÔNG migrate content thật (Hỏa Thế/Kiếm Ý/boss enrage thật) | [2026-09-04-turn-battle-system-slice4-resource-boss-design.md](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice4-resource-boss-design.md) | [2026-09-04-turn-battle-system-slice4-resource-boss.md](../../docs/superpowers/plans/2026-09-04-turn-battle-system-slice4-resource-boss.md) | 🟡 Plan viết xong 2026-09-04, sẵn sàng giao agent thực thi (phụ thuộc Slice 2+3 đã merge trước) |
| Slice 5 — Wave/Stage | `TurnBattle` thêm `wave?: {totalEnemyCount, spawnedCount}`; `TurnBattleSystem` constructor thêm `spawnEnemy?: () => TurnBattleParticipant` (fixture factory, KHÔNG đụng `pickEnemyForSpawn`/Enemy template thật). Spawn quái kế tiếp ngay khi sân trống (`shouldSpawnNextEnemy`); điều kiện thắng đổi sang `isStageComplete()` khi có `wave` — giữ nguyên hành vi cũ (mọi enemy hiện tại chết) khi KHÔNG có `wave`, không phá test Slice 1-4 | [2026-09-04-turn-battle-system-slice5-wave-design.md](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice5-wave-design.md) | [2026-09-04-turn-battle-system-slice5-wave.md](../../docs/superpowers/plans/2026-09-04-turn-battle-system-slice5-wave.md) | 🟡 Plan viết xong 2026-09-04, sẵn sàng giao agent thực thi (phụ thuộc Slice 2+3+4 đã merge trước) |
| Slice 6 — GameManager Cutover | **FLIP THẬT, không chạy song song (2026-09-04)**: thay hẳn `startBattle`/`startStage`/`updateBattleFixedStep`/`getBattle`/`getStageProgress`, retire `BattleSystem.ts`/`HazardZoneSystem.ts`/`UltimateSystem.ts`, viết lại ~16 file `BattleSystem.*.test.ts`. Phạm vi content: TỐI THIỂU CHẠY ĐƯỢC (basic attack mọi build trước, special/ultimate bổ sung sau — Bạt Kiếm Thuật kênh mất tạm, chưa có model channel/chargeSteps). **Stat System deep-think ĐÃ XONG (2026-09-04)** — blocker lý thuyết đã gỡ, nhưng vẫn cần Stat System PLAN+IMPLEMENT xong (không chỉ design) trước khi Slice 6 viết plan được | [2026-09-04-turn-battle-system-slice6-gamemanager-cutover-design.md](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice6-gamemanager-cutover-design.md) | _(CHƯA VIẾT — chờ Stat System plan+implement xong)_ | 🟡 Design xong cả 2 phía (Slice 6 + Stat System) 2026-09-04; chờ Stat System thực thi xong mới viết plan Slice 6; rủi ro cao nhất toàn rework |
| Slice 7 — Manual UI | Click handler thật trên `CombatSkillSlot.vue`; viết lại `CombatSkillPresentation.ts`'s `deriveState()` cho turn-based; Ultimate slot 5 cast y hệt slot thường | [overview](../../docs/superpowers/specs/2026-09-04-battlesystem-replacement-slices-overview.md) §4 | _(chưa viết)_ | 🔴 Đã khoanh scope, chờ Slice 6 xong mới spec (cần contract mới) |

| Hạng mục | Quyết định | Plan | Trạng thái |
|---|---|---|---|
| AOE Shape extension — thêm `'cross'`/`'row'`/`'column'` vào `ActionTargetingSystem.ts`/`CombatAction.ts`, đổi tên `'area'`→`'square'` (23 literal, 9 file), wire `AoeShape.isCellInShape` cho cross. Bounce/TrueShot wiring KHÔNG nằm trong plan này | [2026-09-04-aoe-shape-extension-design.md](../../docs/superpowers/specs/2026-09-04-aoe-shape-extension-design.md) | [2026-09-04-aoe-shape-extension.md](../../docs/superpowers/plans/2026-09-04-aoe-shape-extension.md) | 🟢 Xong, merge master 2026-09-04 (merge commit `35c958f`; full suite 2408/2408, type-check sạch; QA quick report: [2026-09-04-aoe-shape-extension-quick.md](qa/2026-09-04-aoe-shape-extension-quick.md) PASS WITH EVIDENCE) |
| ~~Ultimate-as-loadout-slot~~ — **THAY THẾ bởi mô hình 3-skill (2026-09-04, phiên Slice 2)**: khái niệm "loadout slot" (5 hay 6 ô) không còn — Ultimate giờ đơn giản là `ultimateSkillId`, 1 trong đúng 3 skill role cố định mọi combatant đều có (xem dòng Slice 2). `UltimateSystem.ts`/`BattleSystem.tryPlayerUltimate()`/`KiemTuCombatHud.vue`'s nút Ult vẫn retire hoàn toàn khi cutover, không đổi | Quyết định chốt 2026-09-04 (phiên Slice 2), thay quyết định "slot index 5" cũ | _(gộp — xem dòng Slice 2 và Manual tap-to-cast UI)_ | 🟢 Đã quyết định: là 1 trong 3 skill role cố định, không phải slot riêng trong loadout |
| BuffSystem turn-duration — `TurnBuffSystem.ts` MỚI hoàn toàn (không sửa `BuffSystem.ts` sống — file đó đang chạy real-time combat thật, sửa tại chỗ sẽ phá game). Port `apply`/`update`/stack-mode/convert/CC-check verbatim, đổi field `remainingTime`→`remainingTurns`, `continuousSeconds`→`continuousTurns`, `damagePerSecond`→`damagePerTurn` | [2026-09-04-turn-buff-system-design.md](../../docs/superpowers/specs/2026-09-04-turn-buff-system-design.md) | [2026-09-04-turn-buff-system.md](../../docs/superpowers/plans/2026-09-04-turn-buff-system.md) | 🟢 Xong, merge master 2026-09-04 (merge commit `8545866`; full suite 2426/2426, type-check sạch; QA quick report: [2026-09-04-turn-buff-system-quick.md](qa/2026-09-04-turn-buff-system-quick.md) PASS WITH EVIDENCE — 1 note: convert-on-max-stacks xảy ra khi nextStacks >= maxStacks, đúng 1:1 hệ sống) |
| BuffSystem — port nốt `getActiveModifiers()`/`isRooted()`/`rollOnHitEffects()`/`getStacks()` vào `TurnBuffSystem.ts` (bị loại khỏi plan 2026-09-04 để giữ phạm vi nhỏ — spec chỉ chốt `apply`/`update`/`isStunned`/`isFrozen`) | Đã biết cách làm (port verbatim như 4 method kia), chỉ chưa viết | _(chưa viết)_ | ⚪ Đã biết hướng, chưa có plan |
| BuffSystem — migrate content thật (`game/src/data/buff/buffs.ts`, boss enrage buff trong `TribulationPhase.ts` data, equipment/talent passive buff) từ `BuffDefinition` sang `TurnBuffDefinition` | Chưa bàn — phụ thuộc `TurnBuffSystem` đã có (plan trên) | _(chưa viết)_ | 🔴 Chưa khảo sát/quyết định |
| BuffSystem — cân bằng lại số lượt (turn-count) cho từng buff definition đã migrate — plan 2026-09-04 CHỦ ĐỘNG giữ nguyên số (X giây → X lượt), không tự cân bằng | Chưa bàn — chỉ làm sau khi content đã migrate | _(chưa viết)_ | 🔴 Chưa khảo sát/quyết định (việc nội dung/balance, không phải cơ chế) |
| BuffSystem — presentation/VFX/tooltip hiển thị duration (`combat-vfx-spawner.ts`'s `durationSeconds`, `combat-status-tooltip.ts`'s `"${remainingTime}s"` format) đổi sang hiển thị theo lượt | Chưa bàn — chỉ cần khi `TurnBuffSystem` thật sự lên hình | _(chưa viết)_ | 🔴 Chưa khảo sát/quyết định |
| ReactionManager.ts turn-based conversion — đã event-triggered sẵn (không có deltaSeconds trong logic), nhưng call site cần dời từ vòng lặp real-time BattleSystem sang turn-based; hiện vẫn gọi `BuffSystem`/`BuffPool` SỐNG, cần quyết định đổi sang `TurnBuffSystem`/`TurnBuffPool` khi nào. **Cần thêm**: reaction nào hiện `spawnLavaZone` (vd "Dung Nham" thạch_hóa+bỏng) phải đổi sang áp `TurnBuffSystem` dot buff thay vì spawn zone (xem quyết định "ĐẢO NGƯỢC HazardZoneSystem" ở trên) | [Khảo sát ban đầu](../../docs/superpowers/specs/2026-09-03-turn-based-combat-design.md) ghi nhận logic không cần đổi, nhưng chưa quyết định thời điểm/cách dời call site | _(chưa viết)_ | 🔴 Chưa khảo sát/quyết định |
| SkillEffectResolver.ts's `grantsSwordZone` conversion — skill hiện spawn SwordZone trực tiếp (không qua Reaction), cần đổi sang áp `TurnBuffSystem` dot buff cùng lý do trên | Theo cùng quyết định "ĐẢO NGƯỢC HazardZoneSystem" | _(chưa viết)_ | 🔴 Chưa khảo sát/quyết định |
| ~~HazardZoneSystem N-turn~~ — **ĐẢO NGƯỢC quyết định cũ (2026-09-04, phiên khảo sát 2)**: khái niệm "zone" định vị liên tục (ai đi vào sau vẫn bị trúng) không còn giá trị trong turn-based (không có di chuyển liên tục giữa lượt). KHÔNG xây `TurnHazardZoneSystem` — thay Lava Zone/Sword Zone bằng áp DoT trực tiếp lên tập target đã chọn tại thời điểm cast, qua AOE shape (đã có plan) + `TurnBuffSystem`'s effect `dot` (đã có plan). `HazardZoneSystem.ts`/`LavaZone.ts`/`SwordZone.ts` sống tiếp tục KHÔNG đổi cho tới khi Reaction Engine/SkillEffectResolver thật sự cutover — lúc đó chỉ đổi CALL SITE (spawn zone → apply dot buff), không có primitive mới nào cần build riêng | Quyết định trực tiếp trong phiên brainstorm 2026-09-04 (khảo sát 2), không cần spec/plan riêng — ghi nhận tại đây | _(không cần — không có gì để build)_ | 🟢 Đã quyết định: KHÔNG làm, gộp vào ReactionManager/SkillEffectResolver conversion sau này |
| ~~Gộp 3 mục~~ — **SUPERSEDED bởi Slice 4 (2026-09-04)**: mảng "Resource turn-hooks + Boss afterTurns" giờ có wiring thật trong `TurnBattleSystem` (xem dòng Slice 4) — không còn "blocked chờ Slice 2+ cutover" nữa vì Slice 4 wire NGAY vào engine turn-based mới (fixture, chưa migrate nội dung thật, nhưng cơ chế đã có). MomentumBreak real wiring KHÔNG còn cần — mục đó đã bị bỏ hẳn (xem dòng Slice 4) | [Đã chốt](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice4-resource-boss-design.md) | _(xem plan Slice 4)_ | 🟢 Superseded — xem Slice 4 |
| GameManager external contract — **Khảo sát xong (2026-09-04, phiên 6), KHÔNG thiết kế giải pháp**: đây chính là việc cutover thật (Slice 2+), không tách được standalone. Đã liệt kê đầy đủ: driver `updateBattleFixedStep` (fixed-step 0.1s, catchup trần 30s), 18 bước `BattleSystem.update()`, 3 call site UI ghi thẳng vào BattleSystem (`KiemTuCombatHud.vue`: `setChannelTickSeconds`/`ultAutoEnabled`/`tryPlayerUltimate`), 7 call site UI chỉ đọc `getBattle()` (presentation, chỉ cần thích ứng không cần thiết kế lại), entry point `startBattle`/`startStage` | [2026-09-04-gamemanager-external-contract-survey.md](../../docs/superpowers/specs/2026-09-04-gamemanager-external-contract-survey.md) (khảo sát thuần, không phải spec thiết kế) | _(không viết — chờ Slice 2 có TurnBattleSystem đủ hình hài mới thiết kế contract thay thế)_ | 🟢 Khảo sát xong, sẵn sàng cho Slice 2 — cố ý CHƯA thiết kế giải pháp |
| Stat System conversion — **Deep-think 5 main stat + stat phụ HOÀN THÀNH (2026-09-04, phiên Stat System)**: rà toàn bộ `deriveAttributeModifiers()`, hầu hết stat (attack/defense/accuracyRating/evasionRate/criticalRate/criticalDamage/ailmentResistPercent/maxHp/enduranceThreshold/elemental Power) fit nguyên vẹn turn-based (per-hit, không đổi). `speed = 100 + dexterity×0.15` (quy đổi thuần đơn vị từ attackSpeed cũ, neo nền 100 kiểu Honkai Star Rail SPD, KHÔNG cân bằng lại). `cooldownReduction`/`castSpeedPercent`/`movementSpeed` bỏ hẳn, không thay thế. `hpRegenPerSecond`→`hpRegenPerTurn` (giữ nguyên số). Intelligence chấp nhận còn 2 stat (criticalDamage%/ailmentResistPercent), KHÔNG thêm stat bù (YAGNI) | [2026-09-04-stat-system-turn-based-conversion-design.md](../../docs/superpowers/specs/2026-09-04-stat-system-turn-based-conversion-design.md) | _(chưa viết — plan tiếp theo)_ | 🟡 Design xong 2026-09-04, chờ viết plan — mở khóa Slice 6 |
| Manual tap-to-cast UI (`CombatSkillSlot.vue` và liên quan) — hiện HOÀN TOÀN chưa tồn tại (0 UI thủ công), người chơi chỉ chọn skill, không chọn target (targeting luôn tự động — [spec §4.5](../../docs/superpowers/specs/2026-09-03-turn-based-combat-design.md)). **Nay là 3 nút cố định** (basic/special/ultimate, xem dòng Slice 2 — THAY mô hình "6 ô loadout" cũ), retire `UltimateSystem.ts`/`KiemTuCombatHud.vue`'s nút Ult riêng khi thiết kế/implement UI này. **Khảo sát xong (2026-09-04, phiên 7), KHÔNG thiết kế**: `CombatSkillSlot.vue` thuần presentational (tái dùng được — icon/mask/cast bar/resource badge, không giả định đơn vị giây), hoàn toàn KHÔNG có click handler (phải thêm mới). `CombatSkillPresentation.ts`'s `deriveState()` 100% dựa real-seconds (cadence/cooldown) — cần thiết kế lại hoàn toàn cho turn-based. `RadialSkillSelector.vue` đã có pattern "tap chọn skill từ radial menu" nhưng dùng cho loadout EDITING (Skill Path Panel), không phải live-cast — chỉ là tiền lệ tham khảo | [2026-09-04-manual-cast-ui-survey.md](../../docs/superpowers/specs/2026-09-04-manual-cast-ui-survey.md) (khảo sát thuần, không phải spec thiết kế) | _(không viết — chờ TurnBattleSystem có skill loadout mới thiết kế UI thật)_ | 🟢 Khảo sát xong, sẵn sàng cho khi TurnBattleSystem có skill loadout — cố ý CHƯA thiết kế giải pháp |
| StageWaveSystem turn conversion — `WaveSpawnTrigger.ts` MỚI (2 pure function, không đụng `StageWaveSystem.ts` sống). Quyết định: BỎ HẲN `spawnCountdown`/`spawnIntervalSeconds` (không convert sang lượt) — spawn quái kế tiếp NGAY khi sân trống, không cần throttle thời gian thật nữa trong turn-based | [2026-09-04-wave-spawn-trigger-design.md](../../docs/superpowers/specs/2026-09-04-wave-spawn-trigger-design.md) | [2026-09-04-wave-spawn-trigger.md](../../docs/superpowers/plans/2026-09-04-wave-spawn-trigger.md) | 🟢 Xong, merge master 2026-09-04 (merge commit `90f9cf5` — fast-forward; 7 plan tests + 4 adversarial probes, turn/ suite 100/100, type-check sạch; QA quick report: [2026-09-04-wave-spawn-trigger-quick.md](qa/2026-09-04-wave-spawn-trigger-quick.md) PASS WITH EVIDENCE) |
| Rewrite ~16 file `BattleSystem.*.test.ts` đang pin hành vi real-time | Kỳ vọng: viết lại, không giữ (big-bang) | _(chưa viết)_ | 🔴 Chưa khảo sát/quyết định |

## Ngoài phạm vi (có spec riêng sau, KHÔNG thuộc rework này)

| Hạng mục | Ghi chú |
|---|---|
| Hệ nhân vật phụ (party/companion: main + tối đa 4, recruit, skill riêng, nâng cấp theo bậc/phẩm) | [spec §2/§7](../../docs/superpowers/specs/2026-09-03-turn-based-combat-design.md) — kiến trúc turn engine đã tổng quát cho N combatant/phe, nhưng recruitment/UI/nội dung để spec riêng |
| Equipment/Affix content ngoài 3 buff ghép `movementSpeed`+`attackSpeed` | [Ghi nhận](../../docs/superpowers/specs/2026-09-04-turn-based-combat-survey-and-stat-decisions.md) — chưa quyết định phạm vi |
| Enemy data (`Enemies.ts`) cần giá trị `speed` cho từng enemy | [Ghi nhận](../../docs/superpowers/specs/2026-09-04-turn-based-combat-survey-and-stat-decisions.md) — công việc nội dung, làm sau khi Stat System chốt xong |
| **Pháp Tu Reaction Path (hidden path, như Bạt Kiếm Thuật)** — cơ chế "book" (1 slot chứa nhiều sub-skill cùng hệ Ngũ Hành, bấm chọn 1 hành lúc cast) + AI tình huống (đọc debuff/hành đang áp trên địch để tự chọn hành kích reaction, vd auto Thủy vào Hỏa để kích Bốc Hơi) | [Ghi nhận](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice2-skill-actions-design.md) §5 — ý tưởng gốc của user cho Slice 2, dời thành 1 path ẩn riêng biệt (không phải mainline Pháp Tu Thuần), chưa thiết kế |
| Node Tree / Element Slot / ProgressionNode redesign cho khớp mô hình 3-skill | [Ghi nhận](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice2-skill-actions-design.md) §5 — user xác nhận cần đổi nhưng dời phiên riêng, hệ học/nâng cấp hiện tại giữ nguyên cấu trúc cho tới lúc đó |
| Enemy content migration: `Enemy.specialAttacks[]` → 3-skill shape (basic/special/ultimate) cho từng enemy trong `Enemies.ts` | [Ghi nhận](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice2-skill-actions-design.md) §3/§5 — Slice 2 chỉ chứng minh cơ chế đối xứng, không migrate data thật |
| dpsRatio conversion cho content thật (Dung Nham reaction, Kiếm Trận zone) — cần TRƯỚC KHI `ReactionManager.ts`/`SkillEffectResolver.ts` thật sự cutover khỏi `HazardZoneSystem` | [Ghi nhận](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice3-buff-design.md) §5 — Slice 3 chỉ chứng minh cơ chế bằng fixture, không quy đổi công thức thật |
| `TurnStatModifierEffect`/`TurnOnHitProcEffect` application — cần 1 pass "stats recompute" (base + active modifier → effective stats) cho `TurnBattleSystem`, việc riêng cỡ 1 slice | [Ghi nhận](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice3-buff-design.md) §3/§5 — Slice 3 chỉ wire `dot`+`cc`, 2 effect type còn lại của `TurnBuffTypes.ts` vẫn chưa dùng tới |
| `MomentumBreak.ts` — KHÔNG dùng nữa, không phải hoãn. Cơ chế Break/stagger đã bị cắt khỏi redesign hoàn toàn (2026-09-04) | [Quyết định chốt](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice4-resource-boss-design.md) §2 |
| Resource content migration: quy tắc regen/decay thật của Pháp Tu (Hỏa/Thổ/Kim Thế)/Kiếm Tu (Kiếm Ý/Kiếm Thế) → `TurnResourcePool` thật | [Ghi nhận](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice4-resource-boss-design.md) §5 — Slice 4 chỉ chứng minh cơ chế bằng fixture pool |
| Boss enrage content migration: `TribulationPhase.ts`'s `BossEnrage` (`afterSeconds`→`afterTurns`) + hiệu ứng enrage thật → `TurnBossTrigger`/`TurnBuffDefinition` thật | [Ghi nhận](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice4-resource-boss-design.md) §5 — Slice 4 chỉ chứng minh cơ chế bằng fixture trigger |
| Wave/Stage content migration: `StageWaveSystem.pickEnemyForSpawn()`'s enemy-pool roll/eliteChance/hidden-beast/boss-ở-tầng-10 thật → `spawnEnemy` factory thật cho `TurnBattleSystem` | [Ghi nhận](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice5-wave-design.md) §4 — Slice 5 chỉ chứng minh cơ chế bằng fixture factory, để dành cho Slice 6 cutover |
| Basic-attack content mapping cho 8 build (5 hành Pháp Tu Thuần + Kiếm Tu + Thể Tu + Phàm Nhân) + toàn bộ `Enemy.specialAttacks[]` → `TurnSkillDefinition` — prerequisite BẮT BUỘC trước khi Slice 6 chạy được (không phải optional) | [Ghi nhận](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice6-gamemanager-cutover-design.md) §4 — phạm vi Slice 6 CHỈ tối thiểu chạy được, không cần full parity trước flip |
| Wire `hpRegenPerTurn` vào `TurnBattleSystem` — chưa có call site nào (Slice 4's ResourceTurnHook chỉ generic/fixture, không riêng cho HP) | [Ghi nhận](../../docs/superpowers/specs/2026-09-04-stat-system-turn-based-conversion-design.md) §6 |
| 3 buff ghép `movementSpeed`+`attackSpeed` (haste/slow) cần sửa bỏ phần `movementSpeed`, chỉ giữ `speed` | [Ghi nhận](../../docs/superpowers/specs/2026-09-04-stat-system-turn-based-conversion-design.md) §6 — cùng 3 buff đã ghi nhận từ khảo sát trước |
| Channel skill support (`chargeSteps`/channel execution policy) — Bạt Kiếm Thuật (Kiếm Tu) mất tạm sau flip Slice 6, chưa có model turn-based nào cho channel | [Ghi nhận](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice6-gamemanager-cutover-design.md) §3/§6 — gap đã biết từ phiên Slice 2, giờ xác nhận là regression THẬT SỰ chấp nhận được lúc flip, không phải quên |

## Cách cập nhật roadmap này

Sau mỗi lần khảo sát/brainstorm/viết plan cho một hạng mục:
1. Đổi cột **Trạng thái** (🔴 chưa quyết định → ⚪ đã quyết định chưa có plan → 🟡 đang thực thi → 🟢 xong, kèm ngày).
2. Điền/đổi link **Plan** sang file plan thật vừa viết (`docs/superpowers/plans/...`).
3. Nếu quyết định mới làm lệch mô tả ở cột "Quyết định", cập nhật luôn dòng đó — không để roadmap nói khác plan chi tiết.
