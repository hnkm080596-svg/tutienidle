# Roadmap Chuyển Đổi Combat — Real-time → Turn-Based

> Tài liệu định hướng riêng cho việc chuyển đổi combat từ real-time auto-battler sang turn-based ATB, lập ngày 2026-09-04.
> Mỗi hạng mục có spec/plan riêng (dẫn link bên dưới) — khi làm đến đâu, khảo sát + brainstorm + viết plan riêng cho đúng hạng mục đó, KHÔNG gộp nhiều hệ thống vào một plan (quyết định người dùng, 2026-09-04).
> Khi plan chi tiết và roadmap này lệch nhau, plan chi tiết là nguồn sự thật; cập nhật lại dòng tương ứng ở đây ngay sau đó.
> Tài liệu gốc: [2026-09-03-turn-based-combat-design.md](../../docs/superpowers/specs/2026-09-03-turn-based-combat-design.md) (spec thiết kế đã duyệt) + [2026-09-04-turn-based-combat-survey-and-stat-decisions.md](../../docs/superpowers/specs/2026-09-04-turn-based-combat-survey-and-stat-decisions.md) (khảo sát hệ thống thật + sửa sai lệch so với spec gốc + quyết định Stat).

## Nguyên tắc

- Dự án đang ở dev phase — không cần lo save migration (big-bang, không giữ tương thích ngược).
- Sản xuất/tu luyện (idle) **không đổi** — đây là rework thuần combat.
- Combat không bao giờ mô phỏng offline; "Auto" là chính engine turn-based chạy nhanh hơn, không phải công thức riêng.
- Hệ nhân vật phụ (party/companion) **ngoài phạm vi** toàn bộ rework này — có spec riêng sau.
- Mỗi hạng mục dưới đây khi đến lượt làm: khảo sát codebase hiện tại → brainstorm/chốt quyết định với người dùng → viết plan riêng (`docs/superpowers/plans/...`) → cập nhật dòng trạng thái ở đây kèm link plan.

## Milestone 1 — Foundation (primitives độc lập)

Mục tiêu: dựng các primitive thuần (pure function), test riêng, KHÔNG đụng vào `BattleSystem.ts`/`CombatSystem.ts` hiện có.

| Hạng mục | Plan | Trạng thái |
|---|---|---|
| ActionGauge, TurnQueue, ChannelQueue, AoeShape*, BounceChain, TrueShot, BossTurnTriggers, MomentumBreak, ResourceTurnHook — 9 file mới dưới `game/src/core/battle/turn/` | [2026-09-03-turn-based-combat-foundation.md](../../docs/superpowers/plans/2026-09-03-turn-based-combat-foundation.md) | 🟢 Xong, merge master 2026-09-04 (merge commit `aee253b`; 50/50 test turn/ suite, type-check sạch; QA quick report: [2026-09-04-turn-combat-foundation-quick.md](qa/2026-09-04-turn-combat-foundation-quick.md) PASS WITH EVIDENCE) |

## Milestone 2 — BattleSystem Replacement (gộp mọi hệ liên quan, 2026-09-04)

**Quyết định người dùng 2026-09-04**: không tách "Core System Conversion" thành milestone riêng nữa — không đáng phân biệt. Mọi hệ dưới đây làm CHUNG một đợt lớn xoay quanh việc thay `BattleSystem.update(deltaSeconds)`. Trong đợt này vẫn viết plan riêng cho từng hệ khi đến lượt (không gộp 1 plan khổng lồ), nhưng không còn khái niệm "làm sớm trước BattleSystem" nữa — trọng tâm hiện tại là chính bản thân BattleSystem Replacement.

### BattleSystem Replacement — chia slice (2026-09-04)

`update(deltaSeconds)` (18-bước pipeline, ~2400 dòng) quá lớn cho 1 spec/plan — chia thành các slice dọc, mỗi slice lớp thêm 1 phần hành vi thật lên trên slice trước, mỗi slice có spec+plan riêng.

| Slice | Nội dung | Spec | Plan | Trạng thái |
|---|---|---|---|---|
| Slice 1 — Core Turn Loop | Class mới `TurnBattleSystem` độc lập, headless: 1 player vs N enemy cố định, ATB turn order + targeting §4 (gần nhất-trước-mặt) + basic attack qua `CombatSystem.resolveActionHit` có sẵn. KHÔNG skill/buff/reaction/hazard/wave/UI/Stat speed thật (speed truyền tay qua constructor, tách khỏi quyết định Stat còn treo) | [2026-09-04-turn-battle-system-slice1-core-loop-design.md](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice1-core-loop-design.md) | [2026-09-04-turn-battle-system-slice1-core-loop.md](../../docs/superpowers/plans/2026-09-04-turn-battle-system-slice1-core-loop.md) | 🟢 Xong, merge master 2026-09-04 (merge commit `72eec64`; 8/8 plan tests + 4 adversarial probes, turn/ suite 63/63, type-check sạch; QA quick report: [2026-09-04-turn-battle-system-slice1-quick.md](qa/2026-09-04-turn-battle-system-slice1-quick.md) PASS WITH EVIDENCE) |
| Slice 2 — 3-Skill Action Model | **THAY hẳn bản 6-slot loadout cũ (2026-09-04, phiên sau)**: mỗi combatant (Player VÀ Enemy, đối xứng) có đúng 3 skill role cố định — `basicSkillId` (không cooldown), `specialSkillId`/`ultimateSkillId` (cooldown-lượt, resource-gate). Pháp Tu Thuần map thẳng vào chuỗi skill 1-hành đã có (`getPhapTuThuanElement`/`CHAIN_SKILL_IDS`) — không cần book. CHỈ skill damage thuần (không buff/debuff — hoãn Slice 3). Đổi API `runToCompletion()` → `resolveNextStep()` step-oriented tại đây. KHÔNG gồm migrate content thật (basic/special/ultimate của từng build cụ thể) hay migrate `Enemy.specialAttacks[]` — việc nội dung riêng | [2026-09-04-turn-battle-system-slice2-skill-actions-design.md](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice2-skill-actions-design.md) | [2026-09-04-turn-battle-system-slice2-skill-actions.md](../../docs/superpowers/plans/2026-09-04-turn-battle-system-slice2-skill-actions.md) | 🟢 Xong, merge master 2026-09-04 (merge commit `e3ade36`; TurnSkillAction 22/22 + adversarial 6/6, Slice 1 tests 11/11 giữ nguyên pass, turn/ suite 131/131, type-check sạch; QA quick report: [2026-09-04-turn-battle-system-slice2-quick.md](qa/2026-09-04-turn-battle-system-slice2-quick.md) PASS WITH EVIDENCE) |
| Slice 3 — Buff/CC Wiring + Zone-as-dot | **Khảo sát 2026-09-04 xác nhận `TurnBuffSystem`/`TurnBuffPool`/`TurnBuffTypes` ĐÃ MERGE XONG (Milestone 1, không chỉ có plan)**. Mỗi `TurnBattleParticipant` có `buffs: TurnBuffPool` riêng; `TurnSkillDefinition` thêm `appliesBuff?`; tick buff tại lượt của chính holder (TRƯỚC tickCooldowns/selectAction); CC (stun/freeze) block hành động. Zone-as-dot: skill AOE + `appliesBuff` trỏ `dot` buff = đủ thay Lava/Sword Zone, không cần `TurnHazardZoneSystem`. CHỈ dùng `dpsRatio` (không sửa TurnBuffSystem thêm dạng flat-damage). CHỈ fixture skill/buff (KHÔNG migrate content thật Dung Nham/Kiếm Trận). CHỈ dot+cc effect (KHÔNG statModifier/onHitProc — cần stats-recompute pass chưa có) | [2026-09-04-turn-battle-system-slice3-buff-design.md](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice3-buff-design.md) | [2026-09-04-turn-battle-system-slice3-buff-wiring.md](../../docs/superpowers/plans/2026-09-04-turn-battle-system-slice3-buff-wiring.md) | 🟢 Xong, merge master 2026-09-04 (branch `feat/turn-battle-system-slice345`; turn/ suite 138/138 tại thời điểm slice, type-check sạch; QA quick report: [2026-09-04-turn-battle-system-slice3-quick.md](qa/2026-09-04-turn-battle-system-slice3-quick.md) PASS WITH EVIDENCE — 1 deviation note: CC check TRƯỚC buff tick để stun duration-N block đúng N lượt) |
| Slice 4 — Resource/Boss Triggers | **BỎ HẲN MomentumBreak.ts (2026-09-04, phiên Slice 4)**: cơ chế Break/stagger cắt khỏi redesign — boss giờ chỉ là quái có mechanic riêng qua BUFF. Wire `ResourceTurnHook.applyTurnStartDeltas` (fixture `TurnResourcePool`, tick tại lượt CHÍNH holder) + `BossTurnTriggers.isTurnTriggerReady` (bộ đếm `totalTurnsElapsed` toàn trận, boss tự áp buff qua `TurnBuffSystem` sau N lượt, fire đúng 1 lần). Cả 2 hook UNCONDITIONAL với CC block (khác action). CHỈ fixture, KHÔNG migrate content thật (Hỏa Thế/Kiếm Ý/boss enrage thật) | [2026-09-04-turn-battle-system-slice4-resource-boss-design.md](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice4-resource-boss-design.md) | [2026-09-04-turn-battle-system-slice4-resource-boss.md](../../docs/superpowers/plans/2026-09-04-turn-battle-system-slice4-resource-boss.md) | 🟢 Xong, merge master 2026-09-04 (branch `feat/turn-battle-system-slice345`; turn/ suite 151/151 tại thời điểm slice, type-check sạch; QA quick report: [2026-09-04-turn-battle-system-slice4-quick.md](qa/2026-09-04-turn-battle-system-slice4-quick.md) PASS WITH EVIDENCE) |
| Slice 5 — Wave/Stage | `TurnBattle` thêm `wave?: {totalEnemyCount, spawnedCount}`; `TurnBattleSystem` constructor thêm `spawnEnemy?: () => TurnBattleParticipant` (fixture factory, KHÔNG đụng `pickEnemyForSpawn`/Enemy template thật). Spawn quái kế tiếp ngay khi sân trống (`shouldSpawnNextEnemy`); điều kiện thắng đổi sang `isStageComplete()` khi có `wave` — giữ nguyên hành vi cũ (mọi enemy hiện tại chết) khi KHÔNG có `wave`, không phá test Slice 1-4 | [2026-09-04-turn-battle-system-slice5-wave-design.md](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice5-wave-design.md) | [2026-09-04-turn-battle-system-slice5-wave.md](../../docs/superpowers/plans/2026-09-04-turn-battle-system-slice5-wave.md) | 🟢 Xong, merge master 2026-09-04 (branch `feat/turn-battle-system-slice345`; turn/ suite 161/161 tại thời điểm slice, type-check sạch; QA quick report: [2026-09-04-turn-battle-system-slice5-quick.md](qa/2026-09-04-turn-battle-system-slice5-quick.md) PASS WITH EVIDENCE) |
| Slice 6 — GameManager Cutover | **FLIP THẬT, không chạy song song (2026-09-04)**: thay hẳn `startBattle`/`startStage`/`updateBattleFixedStep`/`getBattle`/`getStageProgress`, retire `BattleSystem.ts`/`HazardZoneSystem.ts`/`UltimateSystem.ts`, viết lại ~16 file `BattleSystem.*.test.ts`. Phạm vi content: TỐI THIỂU CHẠY ĐƯỢC (basic attack mọi build trước, special/ultimate bổ sung sau — Bạt Kiếm Thuật kênh mất tạm, chưa có model channel/chargeSteps). **Đang thực thi qua [Completion plan](../../docs/superpowers/plans/2026-09-04-turn-based-combat-completion.md) Task 8** (branch `feat/turn-unified-battle-flow`): unified battle flow user chốt — **Countdown → Spawn (đứng yên, không di chuyển) → Gauge combat → Wave spawn khi hết enemies trên sân → Result khi hết wave**. Đã xong: `TurnBattleState` thêm `'countdown'` + `tickCountdown()` (30 pacing ticks = 3s hệ sống); `getBattle()` shim trả TurnBattle cho mọi consumer (state khớp `isBattleInProgress`); spawn placement tái dùng `resolveEnemySpawnPosition` (Boss luôn row 4, thường random); rewards shim qua `processDefeatedEnemies`; auto-repeat cycle; completedStageIds; GameManager tests adapt xanh (49/51 — bossSummon + phapTuChain RETIRED kèm note: boss summon per Deep Review §2, chain/Thế/ult glue chờ content migration). Còn lại: retire 27 BattleSystem.\*.test.ts chính thức, retire BattleSystem.ts/HazardZoneSystem.ts/UltimateSystem.ts, UI adapt | [2026-09-04-turn-battle-system-slice6-gamemanager-cutover-design.md](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice6-gamemanager-cutover-design.md) | [2026-09-04-turn-based-combat-completion.md](../../docs/superpowers/plans/2026-09-04-turn-based-combat-completion.md) (Task 8) | 🟢 Task 8+9 HOÀN TẤT (2026-09-04, merge 3982621, branch eat/turn-unified-battle-flow): unified flow Countdown→Spawn→Gauge→Wave→Result hoạt động (TurnBattleState thêm 'countdown' + tickCountdown 30 ticks = 3s hệ sống; spawn placement tái dùng resolveEnemySpawnPosition — Boss row 4, thường random; getBattle() shim trả TurnBattle cho mọi consumer; auto-repeat cycle + completedStageIds); GameManager tests adapt xanh (bossSummon + phapTuChain RETIRED kèm note — boss summon per Deep Review §2, chain/Thế/ult glue chờ content migration); 28 BattleSystem.*.test.ts retire kèm phân loại (Task 9). Verify: turn/ 185/185, battle/ 333/333, TSC 0. **Toàn bộ Completion plan ĐÃ THỰC THI XONG (2026-09-04, merge `70cb22e`)** — Slice 7 UI (Task 10+11), buff registry (Task 13), full verification (Task 14); còn sót 1 việc dọn dẹp riêng: xoá file sống `BattleSystem.ts`/`HazardZoneSystem.ts`/`UltimateSystem.ts` (không chạy nữa) + PillSystem flaky known |
| Slice 7 — Manual UI (slice cuối cùng của BattleSystem Replacement) | Đổi API: `resolveNextStep()` tách thành `peekNextActor()` (tìm ai sắp tới lượt, không resolve) + `resolveActorTurn(battle, actor, forcedSkillSlot?)` (thực thi, có thể ép skill) — `resolveNextStep()` cũ thành wrapper mỏng, không phá test Slice 1-6. Click handler thật trên `CombatSkillSlot.vue` (3 nút cố định basic/special/ultimate). Thực thi QUA 2 NGƯỜN GỘP LẠI (2026-09-04): (a) Completion plan Task 10+11 (merge `70cb22e`) — engine split + manual-mode pause (cache `awaitedManualActor` — tự nhiên tránh double-advance gauge mà plan Task 2 cảnh báo), `buildTurnSkillPresentation` union mới (path riêng), `TurnCombatSkillBar.vue` 3 nút + toggle; (b) smoke-test browser phát hiện 2 bug thật từ Task 8 cutover — StageManager.active không release (refight no-op vĩnh viễn) + HUD watcher crash `reading 'speed'` (getBattle() shim cast TurnBattle) — CẢ HAI ĐÃ FIX kèm regression test. Legacy HUD cũ (3 path HUD + slider + nút Ult) VẪN GIỮ — việc gỡ bỏ theo Slice 7 plan Task 7-8 của master để dành khi clean-up HUD (quyết định người dùng khi đó) | [2026-09-04-turn-battle-system-slice7-manual-ui-design.md](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice7-manual-ui-design.md) | [2026-09-04-turn-battle-system-slice7-manual-ui.md](../../docs/superpowers/plans/2026-09-04-turn-battle-system-slice7-manual-ui.md) (Tasks 4-8/10 vẫn còn — HUD rewrite + gỡ legacy; engine split Task 1-2 ĐÃ xong qua Completion Task 10) | 🟢 Task 10+11 HOÀN TẤT (2026-09-04, merge `70cb22e`): engine split + manual mode + 3 nút UI + turn-order preview + battle log hoạt động (verify browser: refight ✓ countdown ✓ 0 runtime error); turn/ suite 199/199, full suite 2472/2479 (7 fail pre-existing), type-check sạch; QA quick report: [2026-09-04-turn-combat-completion-remaining-quick.md](qa/2026-09-04-turn-combat-completion-remaining-quick.md) PASS WITH EVIDENCE |

| Hạng mục | Quyết định | Plan | Trạng thái |
|---|---|---|---|
| AOE Shape extension — thêm `'cross'`/`'row'`/`'column'` vào `ActionTargetingSystem.ts`/`CombatAction.ts`, đổi tên `'area'`→`'square'` (23 literal, 9 file), wire `AoeShape.isCellInShape` cho cross. Bounce/TrueShot wiring KHÔNG nằm trong plan này | [2026-09-04-aoe-shape-extension-design.md](../../docs/superpowers/specs/2026-09-04-aoe-shape-extension-design.md) | [2026-09-04-aoe-shape-extension.md](../../docs/superpowers/plans/2026-09-04-aoe-shape-extension.md) | 🟢 Xong, merge master 2026-09-04 (merge commit `35c958f`; full suite 2408/2408, type-check sạch; QA quick report: [2026-09-04-aoe-shape-extension-quick.md](qa/2026-09-04-aoe-shape-extension-quick.md) PASS WITH EVIDENCE) |
| ~~Ultimate-as-loadout-slot~~ — **THAY THẾ bởi mô hình 3-skill (2026-09-04, phiên Slice 2)**: khái niệm "loadout slot" (5 hay 6 ô) không còn — Ultimate giờ đơn giản là `ultimateSkillId`, 1 trong đúng 3 skill role cố định mọi combatant đều có (xem dòng Slice 2). `UltimateSystem.ts`/`BattleSystem.tryPlayerUltimate()`/`KiemTuCombatHud.vue`'s nút Ult vẫn retire hoàn toàn khi cutover, không đổi | Quyết định chốt 2026-09-04 (phiên Slice 2), thay quyết định "slot index 5" cũ | _(gộp — xem dòng Slice 2 và Manual tap-to-cast UI)_ | 🟢 Đã quyết định: là 1 trong 3 skill role cố định, không phải slot riêng trong loadout |
| BuffSystem turn-duration — `TurnBuffSystem.ts` MỚI hoàn toàn (không sửa `BuffSystem.ts` sống — file đó đang chạy real-time combat thật, sửa tại chỗ sẽ phá game). Port `apply`/`update`/stack-mode/convert/CC-check verbatim, đổi field `remainingTime`→`remainingTurns`, `continuousSeconds`→`continuousTurns`, `damagePerSecond`→`damagePerTurn` | [2026-09-04-turn-buff-system-design.md](../../docs/superpowers/specs/2026-09-04-turn-buff-system-design.md) | [2026-09-04-turn-buff-system.md](../../docs/superpowers/plans/2026-09-04-turn-buff-system.md) | 🟢 Xong, merge master 2026-09-04 (merge commit `8545866`; full suite 2426/2426, type-check sạch; QA quick report: [2026-09-04-turn-buff-system-quick.md](qa/2026-09-04-turn-buff-system-quick.md) PASS WITH EVIDENCE — 1 note: convert-on-max-stacks xảy ra khi nextStacks >= maxStacks, đúng 1:1 hệ sống) |
| BuffSystem — port nốt `getActiveModifiers()`/`isRooted()`/`rollOnHitEffects()`/`getStacks()` vào `TurnBuffSystem.ts` (bị loại khỏi plan 2026-09-04 để giữ phạm vi nhỏ — spec chỉ chốt `apply`/`update`/`isStunned`/`isFrozen`) | Đã biết cách làm (port verbatim như 4 method kia) | Thực thi qua Completion plan Task 3 | 🟢 Xong (Completion Task 3, merge `3982621`) — cả 4 method + tests đã có trong `TurnBuffSystem.ts`; `getActiveModifiers` được Task 4 (stats recompute) tiêu thụ |
| BuffSystem — migrate content thật (`game/src/data/buff/buffs.ts`, boss enrage buff trong `TribulationPhase.ts` data, equipment/talent passive buff) từ `BuffDefinition` sang `TurnBuffDefinition` | Chưa bàn — phụ thuộc `TurnBuffSystem` đã có (plan trên) | Completion plan Task 13 (merge `70cb22e`) | 🟡 Converter `toTurnBuffDefinition()` + `TURN_BUFF_REGISTRY` (46/46 live buff ids trong `game/src/data/buff/TurnBuffRegistry.ts`, completeness test) xong 2026-09-04; buff trong TribulationPhase/equipment/talent passive CHƯA (còn nữa); registry chưa có consumer production (chờ wire vào TurnBattleSystem) |
| BuffSystem — cân bằng lại số lượt (turn-count) cho từng buff definition đã migrate — plan 2026-09-04 CHỦ ĐỘNG giữ nguyên số (X giây → X lượt), không tự cân bằng | Chưa bàn — chỉ làm sau khi content đã migrate | _(chưa viết)_ | 🟢 Đã chốt policy: giữ nguyên số (X giây → X lượt) — converter Task 13 áp dụng; phần tune thật là việc nội dung sau này |
| BuffSystem — presentation/VFX/tooltip hiển thị duration (`combat-vfx-spawner.ts`'s `durationSeconds`, `combat-status-tooltip.ts`'s `"${remainingTime}s"` format) đổi sang hiển thị theo lượt | Chưa bàn — chỉ cần khi `TurnBuffSystem` thật sự lên hình | _(chưa viết)_ | 🔴 Còn lại — khảo sát 2026-09-04 xác nhận 2 file này phục vụ hệ SỐNG (BuffSystem/HazardZone runtime events), turn engine CHƯA có UI render buff riêng nên chưa có consumer để đổi (hoãn tới khi UI buff turn thật được build) |
| ReactionManager.ts turn-based conversion — đã event-triggered sẵn (không có deltaSeconds trong logic), nhưng call site cần dời từ vòng lặp real-time BattleSystem sang turn-based; hiện vẫn gọi `BuffSystem`/`BuffPool` SỐNG, cần quyết định đổi sang `TurnBuffSystem`/`TurnBuffPool` khi nào. **Cần thêm**: reaction nào hiện `spawnLavaZone` (vd "Dung Nham" thạch_hóa+bỏng) phải đổi sang áp `TurnBuffSystem` dot buff thay vì spawn zone (xem quyết định "ĐẢO NGƯỢC HazardZoneSystem" ở trên) | [Khảo sát ban đầu](../../docs/superpowers/specs/2026-09-03-turn-based-combat-design.md) ghi nhận logic không cần đổi, nhưng chưa quyết định thời điểm/cách dời call site | Completion plan Task 6 (survey, merge `70cb22e`) + Task 7 (definitions, cùng merge) | 🟡 Survey xong 2026-09-04: DPS formulas xác nhận (Dung Nham 20 dmg/tick cố định → dpsRatio 2.0 tại baseline attack 10; Kiếm Trận 0.3×attack → dpsRatio 0.3 1:1). **Phát hiện quan trọng: turn engine KHÔNG gọi ReactionManager/SkillEffectSystem** (không có call site để swap — engine turn chưa wire reaction/skill-effect thật) |
| SkillEffectResolver.ts's `grantsSwordZone` conversion — skill hiện spawn SwordZone trực tiếp (không qua Reaction), cần đổi sang áp `TurnBuffSystem` dot buff cùng lý do trên | Theo cùng quyết định "ĐẢO NGƯỢC HazardZoneSystem" | Completion plan Task 7 Step 1-2 (merge `70cb22e`) | 🟡 Definition `dung_nham_burn`/`kiem_tran_burn` (`game/src/data/buff/TurnBuffs.ts`) + structural tests xong 2026-09-04; swap call site BLOCKED hợp lệ — không có call site trong turn engine (sẽ áp khi reaction thật wire vào turn engine, việc content migration) |
| ~~HazardZoneSystem N-turn~~ — **ĐẢO NGƯỢC quyết định cũ (2026-09-04, phiên khảo sát 2)**: khái niệm "zone" định vị liên tục (ai đi vào sau vẫn bị trúng) không còn giá trị trong turn-based (không có di chuyển liên tục giữa lượt). KHÔNG xây `TurnHazardZoneSystem` — thay Lava Zone/Sword Zone bằng áp DoT trực tiếp lên tập target đã chọn tại thời điểm cast, qua AOE shape (đã có plan) + `TurnBuffSystem`'s effect `dot` (đã có plan). `HazardZoneSystem.ts`/`LavaZone.ts`/`SwordZone.ts` sống tiếp tục KHÔNG đổi cho tới khi Reaction Engine/SkillEffectResolver thật sự cutover — lúc đó chỉ đổi CALL SITE (spawn zone → apply dot buff), không có primitive mới nào cần build riêng | Quyết định trực tiếp trong phiên brainstorm 2026-09-04 (khảo sát 2), không cần spec/plan riêng — ghi nhận tại đây | _(không cần — không có gì để build)_ | 🟢 Đã quyết định: KHÔNG làm, gộp vào ReactionManager/SkillEffectResolver conversion sau này |
| ~~Gộp 3 mục~~ — **SUPERSEDED bởi Slice 4 (2026-09-04)**: mảng "Resource turn-hooks + Boss afterTurns" giờ có wiring thật trong `TurnBattleSystem` (xem dòng Slice 4) — không còn "blocked chờ Slice 2+ cutover" nữa vì Slice 4 wire NGAY vào engine turn-based mới (fixture, chưa migrate nội dung thật, nhưng cơ chế đã có). MomentumBreak real wiring KHÔNG còn cần — mục đó đã bị bỏ hẳn (xem dòng Slice 4) | [Đã chốt](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice4-resource-boss-design.md) | _(xem plan Slice 4)_ | 🟢 Superseded — xem Slice 4 |
| GameManager external contract — **Khảo sát xong (2026-09-04, phiên 6), KHÔNG thiết kế giải pháp**: đây chính là việc cutover thật (Slice 2+), không tách được standalone. Đã liệt kê đầy đủ: driver `updateBattleFixedStep` (fixed-step 0.1s, catchup trần 30s), 18 bước `BattleSystem.update()`, 3 call site UI ghi thẳng vào BattleSystem (`KiemTuCombatHud.vue`: `setChannelTickSeconds`/`ultAutoEnabled`/`tryPlayerUltimate`), 7 call site UI chỉ đọc `getBattle()` (presentation, chỉ cần thích ứng không cần thiết kế lại), entry point `startBattle`/`startStage` | [2026-09-04-gamemanager-external-contract-survey.md](../../docs/superpowers/specs/2026-09-04-gamemanager-external-contract-survey.md) (khảo sát thuần, không phải spec thiết kế) | _(không viết — chờ Slice 2 có TurnBattleSystem đủ hình hài mới thiết kế contract thay thế)_ | 🟢 Khảo sát xong, sẵn sàng cho Slice 2 — cố ý CHƯA thiết kế giải pháp |
| Stat System conversion — **Deep-think 5 main stat + stat phụ HOÀN THÀNH (2026-09-04, phiên Stat System)**: rà toàn bộ `deriveAttributeModifiers()`, hầu hết stat (attack/defense/accuracyRating/evasionRate/criticalRate/criticalDamage/ailmentResistPercent/maxHp/enduranceThreshold/elemental Power) fit nguyên vẹn turn-based (per-hit, không đổi). `speed = 100 + dexterity×0.15` (quy đổi thuần đơn vị từ attackSpeed cũ, neo nền 100 kiểu Honkai Star Rail SPD, KHÔNG cân bằng lại). `cooldownReduction`/`castSpeedPercent`/`movementSpeed` bỏ hẳn, không thay thế. `hpRegenPerSecond`→`hpRegenPerTurn` (giữ nguyên số). Intelligence chấp nhận còn 2 stat (criticalDamage%/ailmentResistPercent), KHÔNG thêm stat bù (YAGNI). **Plan viết xong (2026-09-04)**: quyết định người dùng — KHÔNG bù trừ tỉ lệ cho `BattleSystem.ts` sống (live-game compatibility không phải mục tiêu, coi lỗi compile là con trỏ dẫn đường) — rename thuần túy (cùng số) mọi nơi trừ 2 file công thức gốc (StatBlock/StatCalculator dùng số mới 100/0.15 theo spec); grep khảo sát thật ra ~76 file bị ảnh hưởng, xử lý qua TypeScript compiler làm navigator (Task 6) thay vì liệt kê tay từng file | [2026-09-04-stat-system-turn-based-conversion-design.md](../../docs/superpowers/specs/2026-09-04-stat-system-turn-based-conversion-design.md) | [2026-09-04-stat-system-turn-based-conversion.md](../../docs/superpowers/plans/2026-09-04-stat-system-turn-based-conversion.md) | 🟢 Xong, merge master 2026-09-04 (merge commits `a0ca18b`+`b1251ed`, branch `feat/stat-turn-based-conversion`; 108 files changed, full suite 2522/2523 — 1 flaky pre-existing, type-check sạch; QA quick report: [2026-09-04-stat-conversion-quick.md](qa/2026-09-04-stat-conversion-quick.md) PASS WITH EVIDENCE — Slice 6 blocker ĐÃ GỠ) |
| Manual tap-to-cast UI (`CombatSkillSlot.vue` và liên quan) — người chơi chỉ chọn skill, không chọn target (targeting luôn tự động — [spec §4.5](../../docs/superpowers/specs/2026-09-03-turn-based-combat-design.md)). **3 nút cố định** (basic/special/ultimate, xem dòng Slice 2 — THAY mô hình "6 ô loadout" cũ). **Khảo sát xong (2026-09-04, phiên 7)**: `CombatSkillSlot.vue` thuần presentational (tái dùng được — icon/mask/cast bar/resource badge, không giả định đơn vị giây). `CombatSkillPresentation.ts`'s `deriveState()` 100% dựa real-seconds — cần thiết kế lại hoàn toàn cho turn-based | [2026-09-04-manual-cast-ui-survey.md](../../docs/superpowers/specs/2026-09-04-manual-cast-ui-survey.md) (khảo sát thuần, không phải spec thiết kế) | Thực thi qua Completion plan Task 10 (merge `70cb22e`) | 🟢 **ĐÃ IMPLEMENT** (2026-09-04): `buildTurnSkillPresentation()` union turn-based (`ready`/`not_your_turn`/`cooldown`/`blocked_resource`/`locked`/`empty`) trong CombatSkillPresentation.ts (path mới cạnh path cũ — path real-time giữ cho HUD legacy sống); `TurnCombatSkillBar.vue` (3 nút, disable slot không sẵn sàng, toggle Thủ công/Tự động) wire vào CombatSceneOverlay. Manual/auto toggle persist chưa có (session-only — đánh giá sau khi HUD legacy được gỡ theo Slice 7 plan Task 4-8) |
| StageWaveSystem turn conversion — `WaveSpawnTrigger.ts` MỚI (2 pure function, không đụng `StageWaveSystem.ts` sống). Quyết định: BỎ HẲN `spawnCountdown`/`spawnIntervalSeconds` (không convert sang lượt) — spawn quái kế tiếp NGAY khi sân trống, không cần throttle thời gian thật nữa trong turn-based | [2026-09-04-wave-spawn-trigger-design.md](../../docs/superpowers/specs/2026-09-04-wave-spawn-trigger-design.md) | [2026-09-04-wave-spawn-trigger.md](../../docs/superpowers/plans/2026-09-04-wave-spawn-trigger.md) | 🟢 Xong, merge master 2026-09-04 (merge commit `90f9cf5` — fast-forward; 7 plan tests + 4 adversarial probes, turn/ suite 100/100, type-check sạch; QA quick report: [2026-09-04-wave-spawn-trigger-quick.md](qa/2026-09-04-wave-spawn-trigger-quick.md) PASS WITH EVIDENCE) |
| Rewrite ~16 file `BattleSystem.*.test.ts` đang pin hành vi real-time | Kỳ vọng: viết lại, không giữ (big-bang) | Thực thi qua Completion plan Task 9 | 🟢 Xong (Completion Task 9, merge 3982621) — 28 file retire thành stub "RETIRED" ghi chú phân loại (migrated/dropped-by-design/chờ-content), coverage tương đương nằm ở core/battle/turn/*.test.ts |

## Ngoài phạm vi (có spec riêng sau, KHÔNG thuộc rework này)

| Hạng mục | Ghi chú |
|---|---|
| Hệ nhân vật phụ (party/companion: main + tối đa 4, recruit, skill riêng, nâng cấp theo bậc/phẩm) | **Design xong (2026-09-04)**: [2026-09-04-turn-based-combat-future-systems-design.md](../../docs/superpowers/specs/2026-09-04-turn-based-combat-future-systems-design.md) §6 — `TurnBattle.player` (số ít) → `players: TurnBattleParticipant[]`, chung 1 ATB queue với enemy (tái dùng nguyên `TurnQueue`, không cần primitive mới), thua khi TOÀN BỘ party chết (đối xứng điều kiện thắng). Xác nhận đây LÀ migration cấu trúc lớn nhất trong 5 mục — hầu hết call site của `TurnBattleSystem.ts` đang giả định `player` số ít. Recruitment/UI/nội dung companion vẫn để spec riêng sau, chỉ phần engine đã chốt. [Plan viết xong (2026-09-04)](../../docs/superpowers/plans/2026-09-04-turn-based-combat-future-systems.md) Task 9 (+ Task 10 — fix Slice 7 UI theo sau ngay, bắt buộc cùng phiên) — **🟢 GATE ĐÃ MỞ (2026-09-04)**: cả Combat Fairness Guards (`4356858`) lẫn Completion (`70cb22e`) đều đã merge — toàn bộ Future Systems plan (Task 1-11) sẵn sàng thực thi khi đến lượt. **→ 🟢 ĐÃ THỰC THI + MERGE (2026-09-04, merge từ eat/turn-future-systems)**: Task 9 players[] migration + Task 10 UI party support (pause mọi member, presentation theo paused actor, party status row) + 4 party engine tests — engine-level party ĐANG CHẠY, recruit/UI/companion content vẫn spec riêng sau |
| Equipment/Affix content ngoài 3 buff ghép `movementSpeed`+`attackSpeed` | [Ghi nhận](../../docs/superpowers/specs/2026-09-04-turn-based-combat-survey-and-stat-decisions.md) — chưa quyết định phạm vi |
| Enemy data (`Enemies.ts`) cần giá trị `speed` cho từng enemy | [Ghi nhận](../../docs/superpowers/specs/2026-09-04-turn-based-combat-survey-and-stat-decisions.md) — công việc nội dung, làm sau khi Stat System chốt xong |
| **Pháp Tu Reaction Path (hidden path, như Bạt Kiếm Thuật)** | ~~book + AI tình huống~~ **THAY hẳn (2026-09-04)**: [2026-09-04-turn-based-combat-future-systems-design.md](../../docs/superpowers/specs/2026-09-04-turn-based-combat-future-systems-design.md) §3 — bỏ hẳn book/AI, `special` cast 2 hành random KHÁC nhau mỗi lần (đảm bảo luôn có cơ hội kích reaction, không cần chọn/AI), `ultimate` là buff tự thân N lượt cường hóa dmg reaction. Unlock qua Node Tree's keystone Reaction (§2 dưới). [Plan viết xong (2026-09-04)](../../docs/superpowers/plans/2026-09-04-turn-based-combat-future-systems.md) Task 4-5 — **→ 🟢 ĐÃ THỰC THI + MERGE (2026-09-04, cùng merge)**: selectRandomDistinctElementPair (Fisher-Yates-2) + TurnReactionPathSkills.ts (pool 5 element tái dùng PHAP_TU_BASICS, marker special cooldown 4/mana 20, ultimate self-buff eaction_empowerment 4 lượt +25% — functionally-inert chờ reaction turn-cutover) + double-cast wire trong TurnBattleSystem (marker chặn resolution, 2 hit distinct picks, Sudden Death scaling) |
| Node Tree / Element Slot / ProgressionNode redesign cho khớp mô hình 3-skill | **Design xong (2026-09-04)**: [2026-09-04-turn-based-combat-future-systems-design.md](../../docs/superpowers/specs/2026-09-04-turn-based-combat-future-systems-design.md) §2 — chain 5 skill/hành thiết kế lại xuống đúng 3 (nội dung thật để implement-time); keystone Pure/Reaction GIỮ nhưng nhánh Reaction giờ unlock Reaction Path (dòng trên) thay vì specialization riêng; growth node "cadence" đổi thành bồi dưỡng speed/dexterity. [Plan viết xong (2026-09-04)](../../docs/superpowers/plans/2026-09-04-turn-based-combat-future-systems.md) Task 1-3 — **→ 🟢 ĐÃ THỰC THI + MERGE (2026-09-04, cùng merge)**: CHAIN_SKILL_IDS 5→3 (giữ vị trí A/C/E — fire [hoa_cau_thuat, tam_muoi_chan_hoa, hoa_ha_cuu_thien], v.v.); buildThuanBranch 9 node/hành (2 unlock + biến thể special + ult node prereq special); keystone Reaction Trúc Cơ thêm node eaction_path_unlock_<el> unlock Reaction Path; 6 cadence growth node speed flat 0.03→3 (đúng ngữ nghĩa speed=100 sau Stat conversion). Tổng cây 121→106 node |
| Enemy content migration: `Enemy.specialAttacks[]` → 3-skill shape (basic/special/ultimate) cho từng enemy trong `Enemies.ts` | [Ghi nhận](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice2-skill-actions-design.md) §3/§5 — Slice 2 chỉ chứng minh cơ chế đối xứng, không migrate data thật |
| dpsRatio conversion cho content thật (Dung Nham reaction, Kiếm Trận zone) — cần TRƯỚC KHI `ReactionManager.ts`/`SkillEffectResolver.ts` thật sự cutover khỏi `HazardZoneSystem` | [Ghi nhận](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice3-buff-design.md) §5 — **SỐ LIỆU ĐÃ SURVEY XONG (Completion Task 6, merge `70cb22e`)**: Dung Nham dpsRatio 2.0, Kiếm Trận 0.3 — đã chốt thành definition trong `TurnBuffs.ts`; phần còn lại của mục này là cutover call site (xem dòng ReactionManager/SkillEffectSystem ở bảng trên) |
| `TurnStatModifierEffect`/`TurnOnHitProcEffect` application — cần 1 pass "stats recompute" (base + active modifier → effective stats) cho `TurnBattleSystem`, việc riêng cỡ 1 slice | [Ghi nhận](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice3-buff-design.md) §3/§5 — 🟢 **XONG** (Completion Task 4, merge trước phiên): `TurnStatsRecompute.ts`'s `recomputeEffectiveStats()` wire tại turn start + `rollOnHitEffects` tại hit-resolution |
| `MomentumBreak.ts` — KHÔNG dùng nữa, không phải hoãn. Cơ chế Break/stagger đã bị cắt khỏi redesign hoàn toàn (2026-09-04) | [Quyết định chốt](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice4-resource-boss-design.md) §2 |
| Resource content migration: quy tắc regen/decay thật của Pháp Tu (Hỏa/Thổ/Kim Thế)/Kiếm Tu (Kiếm Ý/Kiếm Thế) → `TurnResourcePool` thật | [Ghi nhận](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice4-resource-boss-design.md) §5 — Slice 4 chỉ chứng minh cơ chế bằng fixture pool |
| Boss enrage content migration: `TribulationPhase.ts`'s `BossEnrage` (`afterSeconds`→`afterTurns`) + hiệu ứng enrage thật → `TurnBossTrigger`/`TurnBuffDefinition` thật | [Ghi nhận](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice4-resource-boss-design.md) §5 — Slice 4 chỉ chứng minh cơ chế bằng fixture trigger |
| Wave/Stage content migration: `StageWaveSystem.pickEnemyForSpawn()`'s enemy-pool roll/eliteChance/hidden-beast/boss-ở-tầng-10 thật → `spawnEnemy` factory thật cho `TurnBattleSystem` | [Ghi nhận](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice5-wave-design.md) §4 — Slice 5 chỉ chứng minh cơ chế bằng fixture factory, để dành cho Slice 6 cutover |
| Basic-attack content mapping cho 8 build (5 hành Pháp Tu Thuần + Kiếm Tu + Thể Tu + Phàm Nhân) + toàn bộ `Enemy.specialAttacks[]` → `TurnSkillDefinition` — prerequisite BẮT BUỘC trước khi Slice 6 chạy được (không phải optional) | [Ghi nhận](../../docs/superpowers/specs/2026-09-04-turn-battle-system-slice6-gamemanager-cutover-design.md) §4 — 🟢 **XONG** (Completion Task 5, merge trước phiên): `game/src/data/skill/TurnBasicAttacks.ts` (`BASIC_ATTACKS_BY_BUILD` + `GENERIC_PHYSICAL_BASIC`), đã wire trong GameManager; enemy `specialAttacks[]` mapping chi tiết còn để làm khi content enemy thật cần |
| Wire `hpRegenPerTurn` vào `TurnBattleSystem` — chưa có call site nào (Slice 4's ResourceTurnHook chỉ generic/fixture, không riêng cho HP) | [Ghi nhận](../../docs/superpowers/specs/2026-09-04-stat-system-turn-based-conversion-design.md) §6 — 🟢 **XONG** (Completion Task 2, merge trước phiên): regen tick tại holder's own turn trong TurnBattleSystem (`TurnBattleSystem.ts` ~L202), clamp maxHp |
| 3 buff ghép `movementSpeed`+`attackSpeed` (haste/slow) cần sửa bỏ phần `movementSpeed`, chỉ giữ `speed` | [Ghi nhận](../../docs/superpowers/specs/2026-09-04-stat-system-turn-based-conversion-design.md) §6 — 🟢 **XONG ngoài dự kiến** (Stat System Task 6 compiler-navigated fixup đã tự dọn sạch) |
| Channel skill support (`chargeSteps`/channel execution policy) — Bạt Kiếm Thuật (Kiếm Tu) mất tạm sau flip Slice 6, chưa có model turn-based nào cho channel | **Design CHỐT LẠI LẦN CUỐI (2026-09-04, thay bản "Thế/Trảm 2 skill riêng" cũ)**: Kiếm Tu `basic` (`tram`/"Huy Kiếm") **giữ nguyên không đổi** — đó chính là "Trảm" theo cách gọi của user, không phải skill mới. Bạt Kiếm Thuật giờ là **1 skill duy nhất** ở vai trò `special`, dùng primitive `chargingTurnsRemaining` (tự áp khi cast, khác hẳn CC bị địch áp, CỐ Ý tách biệt khỏi counter `consecutiveHardCcTurns` của Bá Thể): cast = bắt đầu charge (phase "Thế"), charge xong tự động resolve đòn (phase "Trảm") với sát thương tỉ lệ theo số lượt charge đã author (nội dung tĩnh, không phải charge biến thiên lúc chơi). KHÔNG còn resource pool riêng cho "Thế" — bỏ hẳn framing cũ. [2026-09-04-turn-based-combat-future-systems-design.md](../../docs/superpowers/specs/2026-09-04-turn-based-combat-future-systems-design.md) §5 (bản spec gốc còn ghi mô hình cũ, plan là nguồn sự thật mới hơn — xem commit sửa plan `67de5db`). [Plan viết xong (2026-09-04)](../../docs/superpowers/plans/2026-09-04-turn-based-combat-future-systems.md) Task 7-8 — **→ 🟢 ĐÃ THỰC THI + MERGE (2026-09-04, cùng merge)**: primitive chargingTurnsRemaining/pendingChargedSkillId (charge-init không resolve, tick 1/lượt, resolve đòn khi xong — tách biệt Bá Thể counter, wave/win tail chung); Bạt Kiếm Thuật = 1 skill duy nhất special của Kiếm Tu (chargeTurns 3 = 3s channel, multiplier 3 = base × charge lượt, cooldown 5 authored) qua adapter SPECIALS_BY_BUILD; 	ram basic không đổi |
| Gauge-delta buff effect type (haste/slow đẩy trực tiếp ATB) | **Design xong (2026-09-04)**: [2026-09-04-turn-based-combat-future-systems-design.md](../../docs/superpowers/specs/2026-09-04-turn-based-combat-future-systems-design.md) §4 — effect `gaugeDelta` mới, bắn 1 LẦN tức thì lúc buff được áp (không phải tick liên tục như dot), cộng/trừ % `GAUGE_MAX` thẳng vào actionGauge, khác hẳn statModifier trên speed (vốn là hiệu ứng liên tục). [Plan viết xong (2026-09-04)](../../docs/superpowers/plans/2026-09-04-turn-based-combat-future-systems.md) Task 6 — **→ 🟢 ĐÃ THỰC THI + MERGE (2026-09-04, cùng merge)**: TurnGaugeDeltaEffect (percentOfMax signed) trong cả 2 unions; one-shot push SAU consumeGaugeAfterAction (deferred targets — consume sẽ ghi đè nếu áp trước); refundGauge clamp [0, GAUGE_MAX] |

## Deep Review — Rà soát toàn diện & đối chiếu turn-based kinh điển (2026-09-04, phiên review max-effort)

Phiên này rà lại toàn bộ roadmap so với (a) hệ thống SỐNG hiện tại (khảo sát thật, không đoán) và (b) quy ước phổ biến ở các game turn-based/ATB nổi tiếng (Honkai: Star Rail, Final Fantasy ATB, Persona, XCOM, các game gacha idle turn-based như AFK Arena/Epic Seven) để tìm thiếu sót/rủi ro chưa được ghi nhận.

**Cập nhật 2026-09-04 (phiên review-quyết-định)**: toàn bộ phát hiện dưới đây đã được user duyệt từng mục một — xem bảng tổng hợp cuối phần này để biết quyết định cuối cùng của mỗi mục.

### 1. Sai lệch cần sửa ngay: turn engine KHÔNG hề tổng quát cho N player-side unit

Dòng "Hệ nhân vật phụ" ở bảng Ngoài phạm vi (bên dưới) trích spec gốc nói kiến trúc turn engine "đã tổng quát cho N combatant/phe" — khảo sát thật phiên này cho thấy **điều này SAI với code đã build**: `TurnBattle` có field `player: TurnBattleParticipant` (số ít, không phải mảng) cạnh `enemies: TurnBattleParticipant[]` (`game/src/core/battle/turn/TurnBattleSystem.ts:28-32`); toàn bộ `selectTarget`/`collectTurnTargets` chỉ lặp 1 mảng đối phương cho đúng 1 actor phe kia — không có chỗ nào giả định phe player có >1 đơn vị. Khi Party system thật sự được thiết kế (spec riêng), đây sẽ là một **redesign cấu trúc dữ liệu** của `TurnBattle`/`TurnBattleSystem` (đổi `player` thành mảng, targeting/priority phải chọn giữa nhiều unit phe mình), không phải một bổ sung nhỏ lên trên nền đã tổng quát sẵn. Đã sửa ghi chú ở dòng "Hệ nhân vật phụ" bên dưới.

### 2. Gap thật: TribulationPhase (HP-threshold phase + archetype override + summon) chưa được Slice 4 tính đến

Khảo sát `game/src/core/enemy/TribulationPhase.ts:19-51` + `BattleSystem.ts:946-1172` cho thấy boss AI sống hiện tại phức tạp hơn nhiều so với những gì Slice 4 (Resource/Boss Triggers) đã thiết kế:
- **HP-threshold phase** (`hpThresholdPercent`, không phải turn-count) — boss đổi hành vi khi tụt xuống dưới X% HP, không phải "sau N lượt" như `BossTurnTriggers.isTurnTriggerReady()` (chỉ dùng bộ đếm `totalTurnsElapsed` toàn trận).
- **`archetypeOverride`** — đổi hẳn kiểu hành vi (melee/ranged/caster) khi vào phase mới, ảnh hưởng cách chọn skill/target, KHÔNG chỉ là "tự áp buff" như Slice 4 model.
- **`summonEnemyIds`** — boss triệu hồi thêm quái giữa trận theo HP-threshold, một cơ chế hoàn toàn khác spawn-khi-sân-trống của Slice 5.
Slice 4's `BossTurnTriggers` (turn-count only, single-fire buff) và bảng "Boss enrage content migration" hiện tại (dòng dưới) chỉ nói tới `afterSeconds→afterTurns` — **chưa hề nhắc tới HP-threshold, archetype override, hay summon**.

**Quyết định (2026-09-04, phiên review-quyết-định)**: **KHÔNG xây "Boss Phase System" nào cả** — nhất quán với quyết định bỏ MomentumBreak trước đó, boss chỉ là 1 quái với mechanic riêng thông qua BUFF thiết kế thủ công (không phải 1 engine boss riêng biệt). HP-threshold phase/archetype-switch/summon của `TribulationPhase.ts` KHÔNG migrate nguyên bản sang turn-based — khi cần hiệu ứng tương tự (vd. boss mạnh hơn ở HP thấp), thiết kế bằng buff/trigger đã có (`BossTurnTriggers` turn-count hiện tại là đủ cho "tự áp buff sau N lượt"; nếu cần buff kích hoạt theo %HP thay vì lượt, đó là 1 buff-trigger-condition mới, KHÔNG phải phase-system). `TribulationPhase.ts` sống tiếp tục không đổi cho tới khi nội dung boss thật được thiết kế lại từ đầu bằng buff (việc nội dung, chưa lên lịch).

### 3. Rủi ro correctness: multi-target resolution không re-check tử vong giữa các lượt hit trong cùng 1 skill

`collectTurnTargets` (`game/src/core/battle/turn/TurnSkillAction.ts:198-208`) lọc `opposingSide` theo `entity.alive` MỘT LẦN lúc thu thập target, TRƯỚC khi vòng lặp resolve từng hit chạy — không tìm thấy chỗ nào re-validate `isAlive`/`hp<=0` giữa các hit trong cùng vòng lặp resolution của `TurnBattleSystem.ts`. Với skill AOE nhiều target hoặc (tương lai) bounce-chain, một target có thể đã chết từ hit trước đó trong CÙNG skill nhưng vẫn nhận thêm hit sau — nguy cơ: overkill âm HP, double kill-trigger fire (onKill đã có action engine — [tienhiep-skill-trigger-action-engine.md] memory), double loot nếu onKill gắn loot. **Chưa xảy ra ở Slice 1-2** (basic attack, single target chủ yếu) nhưng sẽ lộ rõ khi Slice 3 wire AOE+buff thật.

**Quyết định**: chưa sửa ngay — ghi nhận, để lại làm task hardening riêng khi content AOE thật thật sự migrate (không fold vào Slice 3, không chặn Slice 3 merge).

### 4. Thiếu cơ chế genre-chuẩn: không có CC diminishing-returns/miễn nhiễm tạm thời

`TurnBuffSystem.isStunned()`/`isFrozen()` (`TurnBuffSystem.ts:258-262`) block hành động tuyệt đối khi có `cc:stun`/`cc:freeze` — không tìm thấy cơ chế giảm-tác-dụng-CC-liên-tiếp (tenacity/diminishing returns) hay cửa sổ miễn nhiễm sau khi bị CC (immunity window), khác với hầu hết game turn-based hiện đại (Persona/HSR/XCOM đều có cơ chế chống combo-CC-khóa-cứng để giữ fairness). Với model hiện tại, nếu enemy có ≥2 nguồn CC nối tiếp, player có thể bị khóa hành động vô thời hạn, 0 counterplay.

**Quyết định (2026-09-04, phiên review-quyết-định) — cơ chế "Bá Thể"**: bất động (dính hard CC — stun/freeze) liên tiếp quá 3 lượt → tự động kích hoạt 1 buff 1-lượt tên **"Bá Thể"**: xóa toàn bộ hiệu ứng CC đang có trên actor, VÀ actor miễn nhiễm hoàn toàn với CC cứng mới trong đúng lượt đó. **Spec+plan viết xong (2026-09-04)**: [2026-09-04-combat-fairness-guards-design.md](../../docs/superpowers/specs/2026-09-04-combat-fairness-guards-design.md) + [2026-09-04-combat-fairness-guards.md](../../docs/superpowers/plans/2026-09-04-combat-fairness-guards.md) — gộp chung 1 spec/1 plan với Sudden Death (mục 6 bên dưới) theo yêu cầu người dùng. Đơn giản hóa từ "áp 1 buff miễn nhiễm" thành `consecutiveHardCcTurns` counter + `TurnBuffPool.clearCcEffects()` (không cần buff/effect kind mới — không có code path nào áp CC mới lên actor giữa lúc CC-check và hành động trong cùng `resolveActorTurn()`, nên buff miễn nhiễm là thừa, YAGNI). Phụ thuộc Slice 3 merge trước — plan chưa thể giao thực thi. **→ 🟢 ĐÃ THỰC THI + MERGE (2026-09-04, merge commit `4356858`, branch `feat/combat-fairness-guards`)**: turn/ suite 171/171, type-check sạch; QA quick report: [2026-09-04-combat-fairness-guards-quick.md](qa/2026-09-04-combat-fairness-guards-quick.md) PASS WITH EVIDENCE (1 learned note: endurance flat-subtract SAU multiplier trong pipeline hệ sống — Sudden Death test phải mô hình hóa `(base+7)×m−7`)

### 5. UX cơ hội (không bắt buộc): không có turn-order preview / battle log — 2 tính năng chuẩn của thể loại

- **Turn-order preview** (dải hiện "ai sắp tới lượt" — đặc trưng HSR/FF ATB hiện đại): `TurnQueue.resolveNextTurn()` (`TurnQueue.ts:23-50`) chỉ trả về ĐÚNG 1 actor kế tiếp mỗi lần gọi, không có API "peek N lượt tới" nào, và không UI nào tham chiếu khái niệm này. Với ATB gauge-fill-rate (không phải fixed initiative order), preview chính xác 100% là khó (gauge có thể đổi do buff/CC giữa chừng) nhưng vẫn làm được xấp xỉ. Đáng cân nhắc cho Slice 7 (Manual UI) vì đúng lúc UI cần cho player thấy "lượt của ai" — nhưng KHÔNG bắt buộc cho MVP tap-to-cast.
- **Battle log** (nhật ký text từng lượt): không tồn tại ở cả hệ sống lẫn thiết kế mới — `ActionFeedbackLog.vue` là log riêng cho crafting/building, không phải combat. Turn-based (khác real-time) rất hợp với log dạng text vì hành động rời rạc, dễ đọc.

**Quyết định (2026-09-04, phiên review-quyết-định)**: **CẢ 2** đưa vào Slice 7 — mở rộng scope Slice 7 (Manual UI) hiện có để gồm cả turn-order preview VÀ battle log, không chỉ 3 nút tap-to-cast. Vì Slice 7 còn chờ Slice 6 merge mới viết plan, spec Slice 7 hiện tại (`2026-09-04-turn-battle-system-slice7-manual-ui-design.md`) cần cập nhật/mở rộng trước khi tới lượt viết plan thật — chưa cập nhật spec ngay phiên này, ghi nhận làm việc cần làm khi quay lại Slice 7.

### 6. Thiếu cơ chế genre-chuẩn khác

- **Gauge-delta buff effect** (haste/slow đẩy trực tiếp thanh ATB, khác với speed stat modifier) — `TurnBuffTypes.ts` hiện chỉ có `dot`/`cc`/`statModifier`/`onHitProc`, không có kiểu "cộng/trừ thẳng vào actionGauge". **Quyết định**: CÓ cần, thêm vào danh sách thiết kế — nhưng chưa vội, gộp chung đợt với `statModifier`/`onHitProc` (đã deferred từ Slice 3) khi thiết kế đầy đủ buff effect types.
- **Extra-turn/revival**: không tồn tại ở hệ sống, không phải thiếu sót (chưa từng có), chỉ ghi nhận là không gian thiết kế tương lai (HSR "Action Advance", Persona "One More") — không cần quyết định gì bây giờ.
- **Sudden Death (thay cho stalemate im lặng ở 10.000 lượt)**: từ lượt 11 trở đi, hệ số sát thương gây ra = `1 + 0.3×(totalTurnsElapsed-10)` (**cộng dồn tuyến tính**, đã chốt công thức 2026-09-04), hệ số hồi máu/khiên nhận = `max(0, 1-0.3×(totalTurnsElapsed-10))`, cả 2 phe. `DEFAULT_MAX_TURNS=10_000` vẫn giữ nguyên làm safety-net cuối cùng, không xóa. **Spec+plan viết xong (2026-09-04)**: [2026-09-04-combat-fairness-guards-design.md](../../docs/superpowers/specs/2026-09-04-combat-fairness-guards-design.md) + [2026-09-04-combat-fairness-guards.md](../../docs/superpowers/plans/2026-09-04-combat-fairness-guards.md) — gộp chung với Bá Thể (mục 4) theo yêu cầu người dùng. Phần damage-dealt tái dùng helper có sẵn `scaleActionDamage()` (`ActionImpactSystem.ts:22-26`, không đụng file sống); phần heal/khiên nhận **chưa có hook nào để wire** (turn-based engine chưa có cơ chế heal/khiên nào cả) — ghi rõ trong plan, không âm thầm bỏ qua. Phụ thuộc Slice 4 merge trước — plan chưa thể giao thực thi. **→ 🟢 ĐÃ THỰC THI + MERGE (2026-09-04, cùng merge commit `4356858`)** — damage-dealt side xong; heal/shield-received side vẫn chờ cơ chế heal/khiên (ghi trong plan "Not Covered")

### 7. Idle-speed: battle speed-up — HỦY x1/x2/x4, thay bằng auto vượt ải (giữ nguyên) + auto repeat (giữ nguyên) + auto-farm Hoàn Mỹ (MỚI, spec+plan viết xong, chờ thực thi)

**Lịch sử quyết định (2026-09-04)**: Ban đầu chốt "mang battle-speed-toggle x1/x2/x4 trở lại" — **huỷ hẳn ý này**, thay bằng 1 cơ chế auto-farm gắn với đánh giá "Hoàn Mỹ" (đổi tên từ "3-sao" ban đầu — bỏ khái niệm sao vì không có thang 1/2 sao, chỉ là điều kiện nhị phân đạt/chưa đạt). Người dùng làm rõ đây là **3 cơ chế auto riêng biệt, không phải 2**:
1. **Auto vượt ải** (`progress`, đã có từ trước) — tự leo lên ải cao nhất phù hợp sức mạnh. **Giữ nguyên, không sửa gì.**
2. **Auto repeat** (`repeat`, đã có từ trước) — lặp lại 1 stage thật (vẫn chạy trận thật qua `TurnBattleSystem`, có thể thua), dành cho người CHƯA đủ mạnh để đạt Hoàn Mỹ nhưng vẫn thắng được, cần farm nguyên liệu. **Giữ nguyên, không sửa gì.**
3. **Auto-farm Hoàn Mỹ (MỚI)** — chỉ mở khoá khi 1 stage đã đạt Hoàn Mỹ; mỗi chu kỳ tốn 1/2 thời gian của lần đạt Hoàn Mỹ đầu tiên, **không chạy trận thật, không hoạt ảnh** — roll thẳng reward. Đây là cơ chế cần thiết kế/xây mới, KHÔNG thay thế 2 mục trên.

**Ràng buộc online/offline (chốt 2026-09-04)**: **CHỈ auto-farm Hoàn Mỹ được nhận reward khi offline** (catch-up kiểu Production). `manual`/`repeat`/`progress` bắt buộc phải online (app đang mở, driven bởi tick loop thật) — đây vốn đã là hành vi hiện tại (combat hoàn toàn ephemeral, không có state nào persist qua lúc đóng app, `StageManager` dùng `deltaSeconds` chứ không phải `Date.now()`), spec mới chỉ cần khẳng định rõ KHÔNG được vô tình phá vỡ điều này khi thêm auto-farm.

**Khảo sát xác nhận (2026-09-04) — mục 1+2 đã carry-over đúng, không cần việc gì**: cả 2 cơ chế cũ đã hoạt động đúng với `TurnBattleSystem` — Slice 6 cutover thực tế đã xong (`GameManager.ts:3051-3053`'s comment xác nhận "TurnBattle là engine DUY NHẤT... Real-time BattleSystem.update() KHÔNG còn chạy"). `GameManager.startStage()` (`GameManager.ts:2808-2820`) nhận `repeatContinuously` → `turnBattleRepeatContinuously` → tự `restartTurnBattleCycle()` (`GameManager.ts:2424`) khi `turnBattle.state === 'victory'` (`GameManager.ts:3077-3085`). `resolveNextProgressStage()` (`ProgressStageResolver.ts:12-33`) sống hoàn toàn ở tầng UI (`CombatVictoryPanel.vue:56-102`), không biết gì về battle tick model.

**Thiết kế mục 3 (Auto-farm Hoàn Mỹ) — spec + plan viết xong 2026-09-04**: [2026-09-04-stage-auto-farm-design.md](../../docs/superpowers/specs/2026-09-04-stage-auto-farm-design.md) + [2026-09-04-stage-auto-farm.md](../../docs/superpowers/plans/2026-09-04-stage-auto-farm.md) — 7 task. Tóm tắt (chi tiết đầy đủ xem spec/plan, không lặp lại ở đây để tránh 2 nguồn sự thật lệch nhau):
- Data model mới trên `PlayerData`: `perfectClearStageIds`, `perfectClearSeconds`, `autoFarmStage` (save v56).
- Điều kiện Hoàn Mỹ: `teamHpLossPercent <= 75 && turnsElapsed < stage.perfectClearTurnLimit`. **Lưu ý (phát hiện lúc viết plan)**: spec ban đầu đoán hook vào `StageWaveSystem.ts:124-126`, nhưng khảo sát cho thấy điểm đó có thể KHÔNG còn chạy cho trận turn-based sau Slice 6 cutover (`syncLegacyBattleState()` set `legacy.state='victory'` trực tiếp, khiến `StageWaveSystem.update()` return sớm trước khi chạm dòng đó) — plan's Task 1 bắt buộc xác nhận bằng code thật trước khi Task 3 hook vào `grantTurnBattleRewards()` (`GameManager.ts:3112-3156`, điểm ĐÃ XÁC NHẬN chạy đúng 1 lần mỗi victory turn-based).
- Cơ chế roll reward: tái dùng nguyên `BattleLootSystem.processDefeatedEnemies()` shim pattern đã có (giống `GameManager.grantTurnBattleRewards()`), không chạy `TurnBattleSystem` thật, không hoạt ảnh.
- Offline/catch-up: tái dùng nguyên `ProductionSystem.settleWorkersOffline` + `GameClock.calculateOfflineTime` — NGOẠI LỆ DUY NHẤT được nhận reward offline trong combat.
- UI: chip thứ 4 (`perfect_farm`) trong `BattleRunMode`, chỉ bật khi stage đã Hoàn Mỹ.
- **Cập nhật 2026-09-04 (phiên thực thi hang loạt)**: Task 1 survey xong — (a) completedStageIds PUSH vẫn chạy cho TurnBattle victory qua grantTurnBattleRewards() victory block. Task 2 XONG (3 field PlayerData + save v56, merge c65bfa3). Task 3 — mechanism wire xong (Stage.perfectClearTurnLimit, turnBattleStartedAtMs, recordPerfectClearIfEligible trong grant victory block) NHƯNG test describe.skip: victory block chạy (emitted=true) mà record không ghi — 1 subtle flow issue chua root-cause (systematic-debugging stop rule sau 4+ hypotheses). Task 4-7 CHUA BAT DAU (cycle roll, offline catch-up, UI chip, verify) — follow-up session

### 8. Không hành động — chỉ ghi nhận / đã xác nhận không phải gap

- **RNG không seed** (`Math.random()` trực tiếp, không có RNG abstraction trong `game/src/core/battle/`) — chỉ quan trọng nếu sau này cần replay/determinism; không phải vấn đề bây giờ.
- **Save/serialization**: xác nhận combat hoàn toàn ephemeral (không có field battle nào trong `GameManagerSaveRestoreDeps`) — nhất quán với nguyên tắc "không mô phỏng offline" đã có, KHÔNG phải gap.
- **Stalemate 10.000-lượt im lặng THUA**: đã xử lý bằng Sudden Death (mục 6) thay vì cần cảnh báo/UI riêng — trận sẽ luôn kết thúc rất lâu trước khi chạm safety-net 10k, nên câu hỏi "phân biệt stalemate-timeout với thua thật" không còn thực sự phát sinh trong gameplay bình thường.
- **Enemy targeting theo archetype (melee/ranged/caster)**: **Đã khảo sát sâu và xác nhận KHÔNG PHẢI gap** (2026-09-04) — `archetype` trong hệ sống chỉ chi phối di chuyển/kiting/telegraph thời gian thực (`EnemyAttackSystem.ts:52,111-181`, `BattleSystem.ts:1243-1277`), KHÔNG BAO GIỜ ảnh hưởng việc CHỌN mục tiêu (quái luôn chỉ có 1 mục tiêu: player/gate — `ActionTargetingSystem.ts:75-85`). Slice 1's "gần nhất-trước-mặt" (`TurnBattleSystem.ts:39-63`) thay thế đúng phần logic không liên quan target-selection, không mất gì.
- **Gap thật lộ ra khi khảo sát mục trên: `CombatAiStrategy` bị mất** — hệ sống có `PlayerData.combatAiStrategy` (người chơi tự chọn `nearest`/`boss_first`/`elite_first`/`lowest_hp`/`highest_hp`, đọc tại `ActionTargetingSystem.ts:92-116`), Slice 1's `selectTarget` không đọc strategy này, luôn cố định gần nhất-trước-mặt. **Quyết định**: **CHẤP NHẬN MẤT** — giữ nguyên "gần nhất-trước-mặt" cố định cho mọi build, không mang `CombatAiStrategy` trở lại, coi đây là 1 downgrade chấp nhận được (cùng loại với "chỉ basic attack lúc flip Slice 6").

| Hạng mục phát hiện phiên review | Mức độ | Quyết định (2026-09-04, phiên review-quyết-định) |
|---|---|---|
| Party turn engine KHÔNG tổng quát N-unit như spec cũ khẳng định | Cao (sai lệch tài liệu) | 🟢 Đã sửa ghi chú; redesign thật để dành khi Party spec tới (không hành động thêm bây giờ) |
| TribulationPhase HP-threshold + archetypeOverride + summon | Cao (gap kiến trúc) | 🟢 **KHÔNG xây Boss Phase System** — boss chỉ là quái + buff thiết kế riêng, nhất quán với quyết định bỏ MomentumBreak |
| Multi-target resolution không re-check tử vong giữa các hit cùng skill | Trung bình (correctness bug tiềm ẩn) | ⚪ Ghi nhận, sửa sau khi lộ rừng thật (task hardening riêng, không chặn Slice 3) |
| Không có CC diminishing-returns/miễn nhiễm tạm thời | Trung bình (fairness) | 🟢 **Cơ chế "Bá Thể"** ĐÃ THỰC THI + MERGE (2026-09-04, merge `4356858`) — `consecutiveHardCcTurns` counter + `clearCcEffects()`, đã thấy trong TurnBattleSystem.ts hiện tại |
| Turn-order preview UI (như HSR/FF) | Thấp→Đã lên lịch | 🟢 Xong (Completion Task 11, merge `70cb22e`) — `peekUpcomingActors()` (không mutate gauge thật) + `TurnOrderStrip.vue` (top-5 lượt kế) |
| Battle log/nhật ký combat theo lượt | Thấp→Đã lên lịch | 🟢 Xong (cùng Task 11) — `battle.log` append-only trong `resolveActorTurn` + `BattleLogPanel.vue` (30 dòng cuối) |
| Gauge-delta buff effect type (haste/slow đẩy trực tiếp ATB) | Thấp | ⚪ Cần, gộp chung đợt thiết kế buff effect types đầy đủ (cùng statModifier/onHitProc deferred) |
| Stalemate ở `DEFAULT_MAX_TURNS=10_000` im lặng tính THUA | Thấp | 🟢 Giải quyết gián tiếp qua **Sudden Death** (xem dòng dưới) — không cần xử lý riêng |
| **Sudden Death** — từ lượt 11, dmg +30%/lượt (cộng dồn), heal/khiên -30%/lượt | Trung bình (mới phát sinh) | 🟢 ĐÃ THỰC THI + MERGE (2026-09-04, merge `4356858`) — `scaleActionDamage` wiring thấy trong TurnBattleSystem.ts hiện tại (heal/khiên side vẫn chờ cơ chế heal/khiên — ghi Not Covered) |
| Battle-speed-toggle (x1/x2/x4) | Thấp | 🟢 **HỦY hẳn** — không làm lại (xem mục 7) |
| Auto-farm Hoàn Mỹ (mode mới, nửa thời gian, không hoạt ảnh, duy nhất offline được reward) | Trung bình (tính năng mới) | 🟡 Task 1-2 XONG + Task 3 mechanism wired (merge c65bfa3); Task 3 tests describe.skip (flow issue chưa root-cause); Task 4-7 chưa bắt đầu (xem mục 7 cập nhật) |
| Enemy targeting theo archetype (melee/ranged/caster) | Trung bình→Đã khảo sát | 🟢 Khảo sát sâu xong — KHÔNG phải gap (archetype chỉ chi phối real-time positioning, không ảnh hưởng target selection) |
| `CombatAiStrategy` (nearest/boss_first/elite_first/lowest_hp/highest_hp) bị mất — phát hiện MỚI từ khảo sát archetype | Trung bình (tính năng thật bị mất) | 🟢 **CHẤP NHẬN MẤT** — giữ cố định "gần nhất-trước-mặt" cho mọi build, không mang strategy-choice trở lại |

## Turn-Based Combat Completion — spec+plan gộp (2026-09-04) — 🟢 ĐÃ THỰC THI XONG

**Theo yêu cầu người dùng**, toàn bộ phần còn lại để hoàn thành rework (trừ Combat Fairness Guards đang thực thi riêng, và trừ Party/Pháp Tu Reaction Path/Node Tree/Channel skill vẫn để spec riêng sau) đã được gộp thành **1 spec** + **1 plan** duy nhất: [2026-09-04-turn-based-combat-completion-design.md](../../docs/superpowers/specs/2026-09-04-turn-based-combat-completion-design.md) + [2026-09-04-turn-based-combat-completion.md](../../docs/superpowers/plans/2026-09-04-turn-based-combat-completion.md) — 14 task, 6 phase theo đúng thứ tự phụ thuộc thật (độc lập trước, Slice 6 cutover ở giữa, hậu-cutover sau cùng).

**Trạng thái cuối (2026-09-04, merge cuối `70cb22e`)**: Task 1-11, 13, 14 — **XONG HẾT**. Task 12 (battle-speed-toggle x1/x2/x4) — **BỊ HỦY** bởi quyết định người dùng sau khi plan viết (roadmap mục 7, commit `6b75510`), thay bằng auto-farm Hoàn Mỹ (xem mục 7). Smoke test browser (Task 14 Step 3) phát hiện + fix thêm 2 bug thật từ Task 8 cutover: StageManager.active không release (refight no-op) + HUD crash `reading 'speed'` (getBattle() shim cast) — chi tiết trong [QA report](qa/2026-09-04-turn-combat-completion-remaining-quick.md). QA quick verdict: PASS WITH EVIDENCE.

**2 phát hiện sửa sai lệch roadmap trong lúc khảo sát**:
- `SkillEffectResolver.ts`'s `grantsSwordZone` (dòng 52 dưới) — **không tồn tại**, field thật là `SkillEffect.grantsSwordZone` xử lý ở `SkillEffectSystem.ts`, không phải `SkillEffectResolver.ts`.
- Số file `BattleSystem.*.test.ts` cần viết lại là **27, không phải ~16** như spec Slice 6 gốc ước tính.

**1 mục đã HOÀN THÀNH ngoài dự kiến**: 3 buff ghép `movementSpeed`+`attackSpeed` (dòng 79 dưới) — khảo sát xác nhận Stat System conversion's Task 6 (compiler-navigated fixup) đã tự dọn sạch `movementSpeed` khỏi các buff này như một tác dụng phụ để giữ build pass. Không còn việc gì cần làm — xóa khỏi phạm vi plan mới.

Nội dung cụ thể từng mục (Slice 6 content prerequisites, Slice 6 cutover, Slice 7 mở rộng turn-order-preview+battle-log, battle-speed-toggle, BuffSystem port+migrate+presentation, ReactionManager/SkillEffectSystem conversion, stats-recompute cho TurnStatModifierEffect/TurnOnHitProcEffect, dpsRatio thật, hpRegenPerTurn wiring, multi-target hardening) — xem spec/plan trên, không lặp lại ở đây để tránh 2 nguồn sự thật lệch nhau.

## Rủi ro liên-plan & vấn đề còn mở (2026-09-04, sau khi viết Future Systems plan)

3 plan lớn hiện đang tồn tại song song ([Combat Fairness Guards](../../docs/superpowers/plans/2026-09-04-combat-fairness-guards.md), [Completion](../../docs/superpowers/plans/2026-09-04-turn-based-combat-completion.md), [Future Systems](../../docs/superpowers/plans/2026-09-04-turn-based-combat-future-systems.md)) — dù mỗi plan tự nó đã self-review, khi xếp CHUNG với nhau vẫn lộ ra vài điểm cần các agent thực thi lưu ý, vì không plan nào tự thấy được xung đột với plan khác khi viết riêng lẻ. **Cập nhật 2026-09-04: Combat Fairness Guards (`4356858`) + Completion (`70cb22e`) đều đã merge — hard gate của Future Systems Phases B-E ĐÃ MỞ, toàn bộ plan (Task 1-11) sẵn sàng thực thi khi đến lượt.**

**Đã tìm thấy và SỬA XONG (không cần hành động thêm)**:
- Future Systems' Task 8 (Channel skill nội dung Kiếm Tu) ban đầu gán "Thế" vào vai trò `basic` của Kiếm Tu — xung đột trực tiếp với Completion plan's Task 5 (đã khảo sát: Kiếm Tu `basic` thật là `tram`/"Huy Kiếm"). **Sửa 2 lần, bản cuối cùng (commit `67de5db`)**: hóa ra `tram` CHÍNH LÀ "Trảm" theo cách gọi của user (không phải skill mới) — `tram` giữ nguyên basic hoàn toàn không đổi; Bạt Kiếm Thuật giờ là 1 skill duy nhất ở `special`, dùng nguyên primitive `chargingTurnsRemaining` (Task 7) làm cả 2 phase Thế→Trảm, KHÔNG cần resource pool riêng như bản nháp giữa chừng từng vẽ ra. Xem dòng "Channel skill support" ở bảng Ngoài phạm vi để biết chi tiết cuối cùng.

**Đã ĐÓNG (commit `1404442`)**:
- Gap "Slice 7 UI xây trước khi Party tồn tại" — đã thêm hẳn **Task 10 mới** vào Future Systems plan (chèn giữa Task 9 và task verify cuối, đổi số cũ 10→11), bắt buộc chạy CÙNG phiên với Task 9, không được để `players[]` merge mà Slice 7 UI im lặng hỏng ở giữa. Task 10 cover: generalize `peekNextActor()`'s player-check thành `battle.players.includes(actor)`, 3 nút skill theo dõi đúng party member đang tạm dừng (không cố định `players[0]`), verify turn-order-preview/battle-log đã tổng quát sẵn, thêm 1 status indicator HP/alive tối thiểu cho party.

**Nội dung/cân bằng còn để placeholder, cần 1 lượt tune thật sau khi cơ chế chạy được**:
- Reaction Path's ultimate — %/số lượt cường hóa dmg reaction (Future Systems Task 4).
- Channel skill's `chargeTurns` thật + hệ số sát thương tỉ lệ theo số lượt charge cho Bạt Kiếm Thuật (Future Systems Task 8 — cần đối chiếu số liệu thật từ `BattleSystem.batKiem.test.ts`'s `createBatKiemThuat()` trước khi bịa số).
- Node Tree's 3 skill cụ thể thay cho chain 5 skill cũ, mỗi hành (Future Systems Task 1) — quyết định nội dung để lúc thực thi, ghi vào commit message theo yêu cầu của Task 1's Step 2.

## Cách cập nhật roadmap này

Sau mỗi lần khảo sát/brainstorm/viết plan cho một hạng mục:
1. Đổi cột **Trạng thái** (🔴 chưa quyết định → ⚪ đã quyết định chưa có plan → 🟡 đang thực thi → 🟢 xong, kèm ngày).
2. Điền/đổi link **Plan** sang file plan thật vừa viết (`docs/superpowers/plans/...`).
3. Nếu quyết định mới làm lệch mô tả ở cột "Quyết định", cập nhật luôn dòng đó — không để roadmap nói khác plan chi tiết.
