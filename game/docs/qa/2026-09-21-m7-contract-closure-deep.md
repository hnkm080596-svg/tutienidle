# M7.5 — Contract Closure Deep Evidence (CON-01 → CON-23)

Date: 2026-09-21 · Branch: `feat/m7-battle-wiring` · Contract: `docs/specs/2026-09-17-combat-systems-contract-spec.md` (FINAL — ACTIVE CONTRACT, v1.6)

Per-invariant deep evidence. Every row records: **verdict**, **implementation evidence** (the code that enforces it, not just the test that observes it), **named automated test(s)**, **relevant file(s)**, and **notes**. A PASS here is never inferred from a green suite — each row names the mechanism and the test that pins it.

Section-level coverage (§1–§105), addenda v1.2–v1.6, and the §104 Definition-of-Done rows are mapped in `2026-09-21-m7-contract-closure-matrix.md`; this document owns the 23 final invariants plus the production-boundary proofs.

## Invariant evidence

| ID | Invariant | Verdict | Implementation evidence | Named test(s) | Notes |
|---|---|---|---|---|---|
| CON-01 | Static definitions contain authored intent only | PASS | `SkillDefinition` is an immutable authored schema (`cadence`/`targeting`/`payload` data, no runtime ids, no closures); the adapter (`LegacySkillAdapter.toTurnSkillDefinition`) lifts `TurnSkillDefinition` into it; `LegacySkillCoverage` census statically enumerates every producer source so a new producer fails until represented. | `SkillDefinition.test.ts` (schema/immutability); `LegacySkillCoverage.test.ts` (census fails on unrepresented producer); `skillDefProducerSources.test.ts` (producer-source enumeration) | The census is the failure-mode proof: adding a production producer without census representation breaks the suite. |
| CON-02 | Runtime operations contain resolved runtime context | PASS | `ResolvedCombatOperation = CombatOperation & { operationId, origin }` — `CombatOperationOrigin` carries `rootActionId`/`parentOperationId`/`causationEventId`/`castId`/`subcastIndex`/`reactionId`; origin is stamped at op mint inside `SkillExecutor`/`CombatScheduler`, never inferred later. | `SkillResolver.test.ts`; `operations.test.ts` (origin stamping + no top-level sourceId); `CombatOperationExecutor.test.ts` | Causality is field-carried, not sequence-inferred (verified structurally by the CON-22 reconstruction test). |
| CON-03 | Cross-authority mutation requests use CombatOperations | PASS | Every battle-reachable cross-domain mutation routes `CombatOperation` → `CombatScheduler` → `CombatOperationExecutor` → authority port. M7.0 inventory §5 audited every `currentHp`/`alive`/`currentWard`/`currentMp`/`currentThe`/`actionGauge`/buff-field write: all sit inside owning authorities, init/restore lanes, or the engine-unit test lane. | `TurnBattleSystem.contract.test.ts` §§91–102 (all mutation flows through scheduler); `inventory` §5 audit; `combatSchedulerDormancy.test.ts` (only sanctioned drivers may call scheduler intake) | M7.5c re-audit: `commitCast`/`grantTheFromCast` direct The writes are engine-unit-lane only (`runtime === undefined`), unreachable in production. |
| CON-04 | Internal authority lifecycle does not require artificial CombatOperations | PASS | `BuffSystem` expiry/duration/stack bookkeeping mutates its own store inside lifecycle authority without minting fake cross-system ops; §102 proves lifecycle ops are not emitted as authored operations. | `BuffLifecycle.test.ts`; `TurnBattleSystem.contract.test.ts` §102 (lifecycle mutation exception) | Same-authority internal lifecycle is allowed by the contract; the boundary is cross-domain mutation. |
| CON-05 | BuffSystem is the sole BuffInstance mutation authority | PASS | The only `BuffSystem` in battle is minted inside `mintCycleScheduler` (`GameManagerTurnBattleOps:1054`); all buff ops route through the executor's `buffs` port; `BuffStore` instance ids are deterministic per battle (`buff.battle.${gen}.${n}`); no `BuffPool`/legacy buff authority exists (file/symbol search: comments only). | `BuffSystemApply.test.ts`; `canonicalBuffSurface.test.ts` (surface guard); M7.5c sweep (no BuffPool symbol) | Kit-clone defs ride the battle-local `BuffRegistry` so node-adjusted payloads win def-id slots without a second authority. |
| CON-06 | ReactionSystem selects/resolves but does not mutate | PASS | `ReactionSystem`/`ReactionDispatcher` produce `ReactionResolution` op lists only; consumption/payoff mutate exclusively through batch ops on the authorities. | `ReactionDispatcher.test.ts` 'returns void when selection produces no candidate'; `ReactionBatch.test.ts` (mutations arrive as ops) | Production reaction stays inert — see boundary proofs below. |
| CON-07 | DamageSystem is the damage/HP authority | PASS | `CombatSystemDamageAdapter` is the sole damage writer on the canonical lane — required `rng` dep, no `Math.random`, no inline mint (arch-guarded). Post-M7.1 there is no direct `resolveActionHit` on the routed lane; engine-lane hit resolution is test-only. | `damageAuthorityRng.test.ts` (adapter never references implicit random; every construction binds existing rng); `CombatSystemDamageAdapter.test.ts`; `TurnBattleSystem.contract.test.ts` | The adapter's declared hit/crit/armor policy rolls consume the shared cycle rng — same stream as production. |
| CON-08 | Gauge authority owns Gauge mutation | PASS | `ActionGauge` mutates `actionGauge` (consume/push/clamp); `GaugeDeltaHandler` owns buff-driven gauge pushes drained post-consume; `ActionGaugeAdapter` is the sole op-level gauge port. | `ActionGauge.test.ts`; executor port routing tests ('push_gauge' routes to gauge port only); `TurnBattleSystem.contract.test.ts` | `TurnOrderPreview` writes only on cloned gauge actors — simulation, not mutation. |
| CON-09 | Elemental Reaction observes only committed Buff state | PASS | `elemental_application_committed` fires post-commit; the dispatcher reads the committed registry state (`ElementalStateRegistry`/`BuffSystem` read ports) — never pre-commit snapshots. | `ReactionDispatcher.test.ts` sequential-visibility rows; `SkillExecutor.test.ts` post-settlement read; contract suite §94 | §94 whole-stack: a read step after apply+reaction sees post-settlement stacks = 0. |
| CON-10 | ElementalApplicationCommitted occurs post-commit | PASS | The committed event is emitted only after the application result commits (stacks/refresh lands in the store); failed applications emit `buff_application_failed` instead. | `BuffAcceptance.test.ts` 'canonical reaction interaction'; contract suite §91 | No committed event exists for a failed apply — the trigger itself is post-commit evidence. |
| CON-11 | `addedStacks > 0` is required for baseline Reaction | PASS | `ReactionTriggerGate` inspects the committed application's `addedStacks`; `addedStacks === 0` (max-stack refresh) never evaluates. | `ReactionTriggerGate.test.ts`; contract suite §92 | §92: duration may refresh while `addedStacks = 0` — refresh is not application. |
| CON-12 | Generic stack mutation is not elemental application | PASS | `AddStacks`/`SetBuffStacks` ops mutate stacks without emitting `elemental_application_committed` — generic mutation cannot masquerade as a new application. | `BuffStacks.test.ts`; contract suite §93 | §93: generic `AddStacks` produces neither application event nor reaction evaluation. |
| CON-13 | Reaction-generated elemental changes are non-reactive by default | PASS | Reaction payoff ops carry reaction provenance; the trigger gate refuses to evaluate reaction-originated applications (no recursive reaction chains). | `ReactionPayoff.noRecursion.test.ts` | Prevents reaction → application → reaction recursion by contract, not luck. |
| CON-14 | Reaction settles before the next authored operation | PASS | Immediate events drain inside the scheduler barrier between authored ops; handler-returned ops complete before the next authored op dispatches. | `CombatScheduler.test.ts` 'handler-returned ops complete before the next authored op'; `ReactionDispatcher.test.ts`; contract suite §98 | Same barrier that sequences multicast subcasts. |
| CON-15 | Multicast settles sequentially | PASS | Each multicast/repeat execution fully settles (ops → events → reactions) before the next queued execution resolves; queued executions drain before any gauge turn. | `SkillSubcast.test.ts`; contract suite §98; `GameManager.skillPipelineJourney.test.ts` (consecutive root-id log entries per cast) | Journey 2 proves it on real authored content: 3 consecutive `da_phap_lien_tuyen` log entries per cast. |
| CON-16 | Reaction batch validates snapshot preconditions before mutation | PASS | `CombatOperationBatchRunner` preflights the batch snapshot (target liveness, selector-instance existence, stack expectations) before the first op; per-op revalidation inside the frame is the shared §49 gate. | `CombatBatchRunner.test.ts`; `CombatScheduler.test.ts` preflight rows | M7.3 finding: the per-op gate now lives in the shared runner — production and headless lanes consult one authority. |
| CON-17 | A stale Reaction is skipped atomically before mutation | PASS | A batch whose preflight fails emits `batchSkips` + `skippedResults` and runs zero ops — no partial consume/payoff. | `ReactionBatch.test.ts` sec.95 rows; contract suite §95; `TurnBattleSystem.determinism.test.ts` stale-batch export row | Stale batches produce both trace lanes — exporter verified. |
| CON-18 | After Reaction batch begins, no rollback occurs | PASS | Once ops start committing, earlier committed ops stay committed and later invalid ops skip (`invalid_target_state`) — no compensating reversal. | `ReactionBatch.test.ts` 'death mid-batch keeps committed ops, trailing op skips'; contract suite §96 | §96 whole-stack evidence: earlier damage committed, trailing `apply_buff` skipped on the dead target. |
| CON-19 | Immediate events are processed exactly once | PASS | The scheduler reserves event ids and faults on repeats; a duplicate immediate event id never re-enters the handler lane. | `CombatScheduler.test.ts` exactly-once rows; contract suite §97 | §97: a duplicated `elemental_application_committed` id produced exactly one reaction, not two. |
| CON-20 | CombatScheduler alone owns combatSequence | PASS | `combatSequence` is allocated at op execution-start inside `CombatScheduler` only; the inventory §6 allocator sweep found no second battle-lane allocator. `BuffPersistence` uses a private counter explicitly documented as out-of-battle (its events never enter a battle trace; its constructor rejects periodic defs). | `CombatScheduler.test.ts` 'op sequence is allocated at execution-START' + ctx.sequence rows; `inventory` §6 | Documented exception: the persistent lane counter is sequence-shaped but scope-isolated. |
| CON-21 | Tie-breaking uses stable authored priority, not ReactionId spelling | PASS | `ReactionRegistry` validates unique priorities at registration; `ReactionSelection` orders candidates by authored priority only. | `ReactionRegistry.test.ts` unique-priority validation; `ReactionSelection.test.ts` | No lexical/id-derived ordering anywhere in selection. |
| CON-22 | All runtime mutations remain causally traceable | PASS | `CombatTrace` records executions/events/faults/batchSkips/skippedResults with `origin.*` causation fields; `CombatTraceExporter` deep-clones all lanes (`structuredClone`) and merges a chronological journal sorted by `combatSequence`; causal fields resolve to real parents, not sequence order. | `TurnBattleSystem.determinism.test.ts` — 'the named chain apply_buff -> committed event -> reaction ops -> resolved event resolves' + 'every causation field resolves to a real parent inside the export'; `CombatTraceExporter.test.ts` multi-root tree; `CombatScheduler.test.ts` causation rows; `ReactionTrace.test.ts` | M7.4: the causal graph was reconstructed from fields alone and every parent resolved. |
| CON-23 | No core system contains path-specific special-case logic | PASS | M7.5c deletion sweep: no per-skill/per-ID handler lanes exist (all `skill.id ===` comparisons are slot/identity matching — charge-id match, NULL_ACTION `''`, basic identity — never content dispatch); no `TurnReactionManager`/`BuffPool`/`SkillToTurnSkillConverter` symbols; the `runtime === undefined` engine lane is the sole remaining legacy execution representation and is test-only, guarded at construction. | `damageAuthorityRng.test.ts` (rng arg required + ≤1 runtime-less production site); `LegacySkillCoverage.test.ts`; `operations.test.ts` (uniform op validation); M7.5c sweep section of the matrix | The engine lane's retention is a documented test seam, not a production path — see boundary proofs. |

## Production boundary proofs

| Boundary | Evidence |
|---|---|
| Single production ACTIVE skill pipeline | All GameManager-level casts route `TurnSkillDefinition` → adapter → `SkillDefinition` → `SkillResolver` → `SkillExecutor` → `CombatScheduler`. `mintCycleScheduler` always mints the runtime; the bootstrap engine (`runtime: undefined`) is inert — `stepTurnBattle` early-returns until `beginBattleCycle` replaces it (structural, not convention). |
| Unsupported runtime casts are loud, never silently legacy | `reportUnroutedCast` warns once per cast identity per battle; the unrouted cast resolves to a no-op on the plan lane; every `runtime`-gated accessor throws on the no-runtime lane; the journey suite asserts zero such warnings on supported content. |
| Production Reaction remains inert | No `new ReactionDispatcher`/`ReactionSystem`/`ReactionRegistry` outside test/fixture code (`grep` verified); `elemental_reaction_enabled` is granted by no production buff definition; `ReactionGate.production.test.ts` proves the minted runtime carries no reaction members; reaction contract coverage uses the fixture composition only. |
| Engine-unit lane is test-only | `damageAuthorityRng.test.ts`: every production `new TurnBattleSystem` injects an explicit `CombatRng` (8th arg) and at most one production site may omit the `TurnCombatRuntime` (5th arg — the documented bootstrap placeholder). A second runtime-less production root fails the suite. |
| BuffPersistence sequence boundary | `BuffPersistence.seq` mints sequence-shaped ordering values for the out-of-battle persistent lane only; its constructor rejects periodic defs (no settlement barrier exists there); nothing it emits enters a battle trace. |

## DoD emphasis items (explicit)

| Item | Evidence |
|---|---|
| Skill damage-policy RNG authority | `operations.test.ts` policy-validation faults; `CombatSystemDamageAdapter.test.ts` policy order on injected rng; `SkillCastCommit.test.ts` rng-spy sees zero hit/crit/armor rolls; `damageAuthorityRng.test.ts` production-root guard. |
| Cleanse limit | `operations.cleanse-limit.test.ts`; `BuffCleanse.test.ts`; `BuffSystem.r4-cleanse.reaudit.test.ts`. |
| Skill cast commit | `SkillCastCommit.test.ts` (all rows); contract suite Part 3 (root cooldown/resource commit once; whiff no-rollback; repeat/multicast/composite no-repay; charge init/deferred resolve); journey suite (exactly-once on real content). |
| Scheduler settlement barriers | `CombatScheduler.test.ts` §55/§56 rows; contract suite §§94/98. |
| Periodic correlation | `CombatScheduler.test.ts` settled-event rows (v1.4/v1.5 — private correlation, canonical-scope ids); `BuffAcceptance.test.ts` manual-tick row. |
| Causal provenance | `TurnBattleSystem.determinism.test.ts` causal-graph pair; `CombatTraceExporter.test.ts`; `CombatScheduler.test.ts` causation rows; `ReactionTrace.test.ts`. |
| Stale Reaction semantics | `ReactionBatch.test.ts` sec.95; contract suite §95; determinism suite stale-batch export. |
| Sequence ownership | `inventory` §6 allocator sweep; `CombatScheduler.test.ts` ctx.sequence rows; `BuffPersistence` documented exception. |

## Verdict

CON-01 → CON-23: **23/23 PASS** with named evidence. No invariant is inferred from suite-level green; each row above names the enforcing mechanism and the test that pins it.
