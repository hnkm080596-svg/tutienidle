# Learning campaigns

Records of qualification campaigns that promote CAPTURED lesson candidates into
policy. Promotion requires (per `docs/qa/protocol/learning.md`): the original bug
killed by a registered guard + legal controls not weakened + sibling hunt with
dispositions + independent qualification review. No lessons are PROMOTED yet —
the first campaign runs when the protocol qualifies a real product change.
