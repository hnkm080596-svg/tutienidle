# Learned Defects

This ledger records evidence-backed defects that should shape future TutienIdle adversarial QA. It starts empty: do not fabricate rows from hypothetical risks, static concerns, or unverified failures.

## Entry Criteria

Only `Confirmed` defects enter this ledger, and a row may be appended only after the report contains the supporting intended failing reproduction test or direct runtime evidence. Record the regression-test path when one exists; otherwise state the evidence-backed reason no regression test is available.

| ID | Component | Trigger pattern | Missed invariant | Why prior QA missed it | Regression test | Domain-pack weighting recommendation |
| --- | --- | --- | --- | --- | --- | --- |
| QA-2026-09-01-001 | Equipment save-shape validation | Persist a new-schema `EquipmentInstance`, then reload the current save | A current-shape equipment snapshot must pass validation and restore | Unit save checks used an empty equipment bag while validator fixtures still described the retired `forgePoints` shape | `game/tests/e2e/save-reload.spec.ts` (existing test exposes the failure) | Any persistent-field schema bridge should weight a non-empty current-save round trip and validator fixture audit as mandatory, even when migration is deferred |
| QA-2026-09-01-002 | Save import handoff | Normalize legacy equipment during import, then write another save or fail the marker write | A cross-reload side channel must be bound to its owning payload and initialized before destructive persistence | Initial QA covered direct import-to-reload only, not pagehide/autosave reordering or degraded storage at the side-channel boundary | `game/src/services/save/SaveSystem.test.ts` | Weight payload identity, intervening writes, and failure at every ordered persistence step whenever state is handed across reload outside the main schema |
| QA-2026-09-01-003 | Equipment base-affix allocation | Request a prefix-first count on a slot whose preferred kind has insufficient eligible stats | A rolled count must use combined compatible unique-stat capacity, with explicit kind fallback, instead of silently truncating | Initial count tests exercised only weapon capacity and asserted total length without all-slot composition | `game/src/core/equipment/EquipmentSystem.test.ts` | For slot-policy loot generation, weight every production slot at minimum/maximum counts and assert literal kind composition plus uniqueness |
| QA-2026-09-01-004 | Equipment Exalted affix allocation | Tiên Exalted chance succeeds when one kind is incompatible or the base roll can consume the sole supreme stat | A successful bonus roll must reserve and produce exactly one compatible tier-5 line in addition to base lines | Initial Exalted coverage used only a weapon RNG sequence that happened to choose its compatible kind and a zero base count | `game/src/core/equipment/EquipmentSystem.test.ts` | For bonus-pool rolls, weight candidate compatibility, wrong-kind RNG, and base/bonus resource competition as mandatory reorder cases |
| QA-2026-09-01-005 | Equipment normal-affix tier gating | Allowed-pool affix has no tier at or below the item's quality cap | Candidate eligibility must include tier availability before random choice; no-tier definitions must not substitute their first tier or block fallback | Production data all had low tiers, while integration coverage used Tiên and production-derived cap/pool expectations | `game/src/core/equipment/EquipmentSystem.test.ts` | Weight literal lower-quality pool/cap fixtures plus malformed/future affix ladders, including same-kind and opposite-kind fallback |
| QA-2026-09-01-006 | Unified equipment essence reward application | Accepted manual or automatic dissolve when the unified material stack is at its registered capacity | A destructive item transition must not complete unless its full reward can be credited | Initial QA checked the catalog limit and a low-balance save path, but did not cross the reachable capacity through each destructive caller | `game/src/core/game/GameManager.dissolveUnifiedEssence.test.ts` | For destructive reward conversions, weight capacity minus/at/plus boundaries across normal, automatic, and restore callers; observe both the removed source and credited sink |
| QA-2026-09-01-007 | Equipment-hall Refine copy | Render either supported locale after replacing symmetric variance with monotonic growth | Paid-action rule text must expose the exact eligibility, direction, range, and clamp contract in every locale | Initial QA verified the computed cost owner but did not assert the rendered rule string, so legacy ±20 wording survived the behavior rewrite | `game/src/components/panels/EquipmentHallPanel.test.ts` | For paid gameplay actions, weight rendered contract copy in every available locale and scan production comments/strings for retired mechanics |
| QA-2026-09-01-008 | Refine preview/commit transaction | Call commit without preview, alter payload/current affixes, retry/replay, or remove and re-add the item | A paid preview must be an exact one-shot capability bound to the generated payload and unchanged source snapshot | Initial QA checked preview/commit cost separation but trusted caller-supplied values and did not attack the public commit boundary | `game/src/core/equipment/EquipmentSystem.test.ts` | For every paid preview/commit API, weight missing provenance, exact payload binding, stale source snapshots, capability consumption on every attempt, replay, and failed-preview invalidation |
| QA-2026-09-01-009 | Refine raw values and save validation | Refine an out-of-range/non-finite raw affix or serialize NaN/Infinity through JSON | Eligibility must use finite raw current values; malformed current numeric state must fail before RNG/spend and at the load boundary | Initial tests used in-range production fixtures and inherited effective-value normalization, while save round trips covered only valid/empty affix arrays | `game/src/core/equipment/EquipmentSystem.test.ts`, `game/src/services/save/saveShapeValidation.test.ts`, `game/src/services/save/SaveRoundTrip.test.ts` | For numeric progression transforms, weight raw below/at/above bounds plus NaN/±Infinity before normalization, and verify JSON's non-finite-to-null conversion is rejected by current-shape validation |
| QA-2026-09-01-010 | Current equipment save validation/restore | Load a current entry with incomplete `StatModifier`/`RolledAffix` shape or an affix ID removed from the runtime registry | Structurally malformed current entries must fail before restore, and registry-dependent unknown IDs must never reach a throwing lookup | Prior checks stopped at `mainStat` being an object and affix `value` being finite; valid round trips did not attack required nested fields or catalog removal | `game/src/services/save/saveShapeValidation.test.ts`, `game/src/services/save/SaveSystem.test.ts`, `game/src/core/game/GameManager.refineTransaction.test.ts` | For persisted polymorphic/nested records, enumerate every required and optional numeric field and separately attack runtime-registry drift at restore |
| QA-2026-09-01-011 | Refine preview provenance and inventory lifecycle | Replace an item with the same ID, remove/re-add the same object, mutate non-affix state, dissolve it, or cancel/supersede its preview | A paid preview is an exact one-shot capability bound to object identity, current membership generation, all relevant source state, and UI lifecycle | Initial provenance bound only a string ID plus affixes; tests consumed the capability while absent before re-adding, which accidentally hid direct lifecycle resurrection | `game/src/core/equipment/EquipmentSystem.test.ts`, `game/src/core/equipment/EquipmentBag.autoDissolve.test.ts`, `game/src/components/panels/EquipmentHallPanel.test.ts` | For paid capabilities over mutable inventory, weight same-ID replacement, same-object lifecycle generations, all non-payload state, destructive removal, failed action on another object, explicit cancel, and component teardown |
| QA-2026-09-01-012 | Refine RNG transaction boundary | Inject a finite RNG result below zero or above one, including after a valid earlier line | A probability source must be finite and within its declared domain before any irreversible multi-owner debit | Endpoint/independence tests used only valid `[0,1]` rolls; finite-domain validation was assumed from finiteness alone | `game/src/core/equipment/EquipmentSystem.test.ts`, `game/src/core/presentation/ActionAvailability.test.ts` | For injectable randomness, always test both accepted endpoints, just-outside finite values, nonfinite values, and a late invalid value in a multi-roll transaction |
| QA-2026-09-01-013 | Current equipment registry restore | Restore a current save containing a later unknown equipment template or affix after valid materials/equipment | Registry drift must hard-fail before every restore owner mutation; silently filtering an owned current entry is data loss, not recovery | Earlier QA treated “does not crash” and item omission as success without asserting current-data preservation or restore-wide atomicity | `game/src/core/game/GameManager.refineTransaction.test.ts` | For registry-backed current saves, weight unknown references after valid entries and observe every owner that appears earlier in restore order; never equate silent current-item loss with safe recovery |
| QA-2026-09-01-014 | Equipment save slot validation | Persist an equipment or equipment-slot entry whose slot is a nonempty unknown string | Fixed-domain identifiers must be validated against the canonical enum before state owners consume them | Shape coverage checked primitive types but not membership in `EQUIPMENT_SLOTS`; valid fixtures used only canonical slots | `game/src/services/save/saveShapeValidation.test.ts`, `game/src/services/save/SaveRoundTrip.test.ts` | For persisted IDs/enums, weight type-valid unknown strings at both direct-validation and JSON round-trip boundaries, including every owner that consumes the field |
| QA-2026-09-01-015 | Refine preview provenance | Change, add, or remove a `mainStat` field after paying for preview | A capability over mutable equipment must snapshot the full actual state contract, not only fields the action writes | Prior provenance review enumerated item and affix fields but omitted the nested main-stat source that affects equipped output | `game/src/core/equipment/EquipmentSystem.test.ts` | For mutable capability snapshots, derive a field-complete mutation matrix from the actual type, including optional additions/removals and nested source identity |
| QA-2026-09-01-016 | Refine runtime commit payload | Pass null, primitive, missing, extra, reordered, duplicate, or replayed entries across the TypeScript boundary | Public runtime boundaries must reject malformed unknown shapes without throwing, mutating, or preserving replay capability | Prior tamper tests stayed inside statically valid object shapes, so dereference safety and exact key shape were assumed | `game/src/core/equipment/EquipmentSystem.test.ts` | For public transaction commits, cast `unknown` adversarial payloads and assert no throw, exact-shape rejection, atomic state, and capability consumption alongside semantic tamper cases |
| QA-2026-09-01-017 | App/Pinia current-save boot orchestration | Boot a shape-valid current save whose equipment contains an unknown runtime template or affix ID | Registry-dependent validation must run before every restore owner, including Pinia restore and active-player assignment, and boot rejection must be handled rather than escape startup | Earlier atomicity regressions called `GameManager.restoreFromSave` directly and never executed the real outer App/Pinia restore ordering | `game/src/services/save/SaveSystem.bootRestore.test.ts` | Whenever a manager adds local preflight, execute the real outer boot/store sequence and snapshot every owner that runs before or after it; require controlled rejection plus whole-session nonmutation |
| QA-2026-09-01-018 | Refine preview provenance | Add, change, or remove optional `EquipmentInstance.realmLevel`, `zoneId`, or `icon` after paying for preview | A paid capability must snapshot the complete actual equipment type, including optional metadata and both undefined-to-defined and defined-to-undefined transitions | The earlier field-complete matrix expanded nested `mainStat` but still omitted optional top-level metadata that does not directly affect the Refine calculation | `game/src/core/equipment/EquipmentSystem.test.ts` | Derive provenance mutation matrices from the full source type and explicitly cover every optional field in both presence directions, even when the action does not write that field |
| QA-2026-09-01-019 | Refine runtime commit payload container | Pass a top-level null, object, string, or number instead of the values array | Public runtime boundaries must validate the outer container before reading length or invoking array methods; rejection must not throw or mutate and must consume the capability | Earlier adversarial payload tests attacked entries inside a valid array, leaving the outer container assumed by TypeScript | `game/src/core/equipment/EquipmentSystem.test.ts` | Attack both outer containers and inner elements at transaction boundaries; assert no throw, exact rejection, atomic state, and one-shot capability consumption for each malformed runtime shape |
| QA-2026-09-02-001 | `GameManager.chooseCultivationPath` | Player has equipment equipped in `mortal` realm, then `chooseCultivationPath('phap_tu' \| 'kiem_tu', player)` advances `realmId` to `qi_refining` | Realm-change boundary must clear equipped items before any other cross-system state mutation; the `unequipAllEquipment()` contract established by `useTribulation.ts:164` is the only enforced path | The T17 (rework P5) tests only covered the tribulation breakthrough path; `chooseCultivationPath` was a separate code path with no T17 follow-up. Both paths change the realm, but only one clears gear. The T16 grade gate then traps the player if they manually unequip. | `game/src/core/game/GameManager.realmAdvanceUnequip.test.ts` (new — fails for the intended reason: weapon stays `equipped: true` after `chooseCultivationPath`) | For every realm-change API, weight all callers as mandatory for the unequip contract; one missed caller makes the entire grade-gate subsystem leaky. Treat the unequip call as a required pre-sync-pass hook at the realm boundary, not as a tribulation-specific detail. |

