// Cờ test tạm thời (2026-08-20) — set true để bỏ qua MỌI gate Building/
// cảnh giới, cho phép thử toàn bộ hệ thống trong game (Luyện Khí,
// Thiên Công, Tứ Nghệ...) mà không cần grind thật. KHÔNG có UI toggle
// cố ý — đây là 1 flag dev tạm thời, đổi tay ở đây rồi build lại là
// đủ. Đặt về false khi hết cần test.
// Fix (2026-08-24) — về false: flag đã bật suốt từ 2026-08-20, bypass
// gate Building/realm trên bản chạy thực tế (người chơi xây miễn phí
// mọi công trình). Bật tay lại khi cần test nội bộ.
export const TEST_MODE_UNLOCK_ALL = false
